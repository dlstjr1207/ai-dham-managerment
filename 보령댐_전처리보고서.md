# 보령댐 방류량 예측 데이터 전처리 보고서

## 1. 목적

본 보고서는 20개 다목적댐 전체 모델링을 수행하기 전에, 예시 대상인 보령댐 데이터를 기준으로 방류량 예측 모델 학습에 필요한 데이터 전처리 과정을 정리한 문서이다.

예측 모델은 이후 XGBoost 회귀 모델을 사용할 예정이며, 본 단계에서는 모델 학습 자체보다 다음 사항을 먼저 확인하였다.

- 댐 운영 데이터와 강수량 데이터가 시간 기준으로 연결 가능한지 확인
- 두 데이터의 시간 범위와 누락 여부 확인
- 강수량 원천 CSV의 구조를 해석하고 학습 가능한 형태로 변환
- 방류량 예측에 사용할 수 있는 기초 피처와 강수량 파생 피처 생성
- 총방류량과 주요 변수 간 상관관계 확인

## 2. 사용 데이터

### 2.1 보령댐 운영 데이터

보령댐 운영 데이터는 PostgreSQL `aidam` 데이터베이스의 `public.dam_operation_data` 테이블에서 가져왔다.

- 대상 댐 코드: `3203110`
- 대상 댐 이름: 보령
- 시간 범위: `2023-01-01 01:00:00` ~ `2026-05-07 11:00:00`
- 총 행 수: 29,339건
- 시간 중복: 0건

사용한 주요 컬럼은 다음과 같다.

| 컬럼명 | 의미 | 모델 활용 방향 |
|---|---|---|
| `obsrdt` | 관측 시각 | 강수량 데이터와 병합 기준 |
| `inflowqy` | 유입량 | 입력 변수 후보 |
| `lowlevel` | 저수위 | 입력 변수 후보 |
| `rf` | 댐 운영 데이터 내 강우량 | 입력 변수 후보 |
| `rsvwtqy` | 저수량 | 입력 변수 후보 |
| `rsvwtrt` | 저수율 | 입력 변수 후보 |
| `totdcwtrqy` | 총방류량 | 예측 목표값 |

본 예측 실험에서는 `totdcwtrqy`를 목표값으로 두고, 나머지 운영 변수와 강수량 파생 변수를 입력값으로 사용하는 방향을 검토하였다.

### 2.2 보령댐 1시간 강수량 CSV

강수량 데이터는 사용자가 제공한 보령댐 1시간 강수량 CSV 파일을 사용하였다.

- 파일명: `보령댐_1시간강수량_20221231_20260507.csv`
- CSV 헤더 위치 정보: `location:54_97`
- 강수량 원천 행 수: 654,948건
- 강수량 기준 시각 범위: `2022-12-31 02:00:00` ~ `2026-05-07 23:00:00`
- 강수량 유효 시각 범위: `2022-12-31 08:00:00` ~ `2026-05-11 15:00:00`
- 유효 시각 기준 고유 시간 수: 29,440건

주의할 점은 CSV가 단순한 표 형태가 아니라는 것이다. 파일 안에는 다음과 같은 일별 시작 구분 줄이 반복되어 있었다.

```text
Start : YYYYMMDD
```

또한 실제 강수량 행은 다음과 같은 구조였다.

```text
day,hour,forecast,value
```

여기서 `forecast`는 예보 리드타임이며, 실제 강수량을 댐 운영 데이터의 관측 시각과 연결하기 위해 기준 시각과 예보 리드타임을 이용해 유효 시각을 계산하였다.

## 3. 강수량 CSV 파싱 방식

강수량 CSV는 일반적인 `read_csv`만으로는 정확하게 시간 컬럼을 만들기 어려웠다. 이유는 다음과 같다.

- 파일 맨 위에 전체 포맷 설명 줄이 있음
- 파일 중간중간 `Start : YYYYMMDD` 줄이 반복됨
- 일부 행의 `day` 값이 깨진 숫자로 들어간 구간이 있음
- `forecast` 값이 `+6`, `+7`처럼 문자열 형태로 들어감

따라서 다음 순서로 직접 파싱하였다.

1. 파일을 한 줄씩 읽는다.
2. `location:`이 포함된 줄에서 강수량 격자 위치를 추출한다.
3. `Start : YYYYMMDD`가 나오면 현재 기준 날짜를 해당 날짜로 갱신한다.
4. 실제 데이터 행에서는 `hour`, `forecast`, `value`만 사용한다.
5. `hour`의 앞 두 자리를 기준 시간의 시각으로 변환한다.
6. `forecast`에서 `+` 기호를 제거하고 정수형 예보 리드타임으로 변환한다.
7. `value`를 실수형 강수량(mm)으로 변환한다.
8. 기준 시각과 예보 리드타임을 더해 실제 적용 시각인 `valid_time`을 생성한다.

시간 생성 방식은 다음과 같다.

```text
base_time = Start 날짜 + hour
valid_time = base_time + forecast_hour
```

이후 `valid_time`을 댐 운영 데이터의 `obsrdt`와 병합하기 위한 기준 시간으로 사용하였다.

## 4. 강수량 대표값 선택 방식

강수량 CSV는 예보 리드타임 구조를 가지고 있기 때문에, 하나의 유효 시각에 대해 여러 개의 예보값이 존재할 수 있다.

예를 들어 동일한 `2023-01-02 12:00:00` 시각에 대해 `+6`, `+7`, `+8` 등 서로 다른 리드타임에서 계산된 값이 있을 수 있다.

이번 1차 실험에서는 다음 기준을 적용하였다.

- 같은 유효 시각에 여러 강수량 값이 있으면 예보 리드타임이 가장 짧은 값을 대표값으로 사용
- 즉, 해당 시각에 가장 가까운 기준 시각에서 생성된 예보값을 사용

이 방식은 추후 실시간 예측 상황에서도 자연스럽게 적용할 수 있다. 모델이 미래 방류량을 예측할 때는 예측 시점에서 사용 가능한 최신 예보 강수량을 입력값으로 사용할 수 있기 때문이다.

## 5. 운영 데이터와 강수량 데이터 병합

병합 기준은 시간 컬럼이다.

| 데이터 | 시간 컬럼 |
|---|---|
| 보령댐 운영 데이터 | `obsrdt` |
| 강수량 데이터 | `valid_time`을 `obsrdt`로 변경 |

병합 결과는 다음과 같다.

- 운영 데이터 행 수: 29,339건
- 병합 후 행 수: 29,339건
- 강수량 매칭 성공 행 수: 29,339건
- 강수량 누락 행 수: 0건
- 매칭률: 100.0%

따라서 보령댐 기준으로는 운영 데이터와 강수량 데이터가 시간 기준으로 완전히 연결된다.

## 6. 생성한 강수량 파생 변수

방류량은 현재 시각의 강수량만으로 결정되지 않는다. 비가 온 뒤 유역에서 물이 모이고 댐으로 유입되기까지 시간이 걸리며, 댐 운영 또한 저수위, 저수율, 유입량 등을 함께 고려한다.

따라서 현재 강수량뿐 아니라 과거 강수량과 누적 강수량을 파생 변수로 생성하였다.

### 6.1 현재 강수량

| 컬럼명 | 설명 |
|---|---|
| `rain_mm` | 해당 관측 시각의 1시간 강수량 |

### 6.2 지연 강수량

과거 특정 시점의 강수량을 입력 변수로 사용하기 위해 lag 변수를 만들었다.

| 컬럼명 | 설명 |
|---|---|
| `rain_lag_1h` | 1시간 전 강수량 |
| `rain_lag_2h` | 2시간 전 강수량 |
| `rain_lag_3h` | 3시간 전 강수량 |
| `rain_lag_6h` | 6시간 전 강수량 |
| `rain_lag_12h` | 12시간 전 강수량 |
| `rain_lag_24h` | 24시간 전 강수량 |

### 6.3 누적 강수량

최근 일정 시간 동안의 총강수량을 입력 변수로 사용하기 위해 rolling sum 변수를 만들었다.

| 컬럼명 | 설명 |
|---|---|
| `rain_sum_3h` | 최근 3시간 누적 강수량 |
| `rain_sum_6h` | 최근 6시간 누적 강수량 |
| `rain_sum_12h` | 최근 12시간 누적 강수량 |
| `rain_sum_24h` | 최근 24시간 누적 강수량 |
| `rain_sum_48h` | 최근 48시간 누적 강수량 |
| `rain_sum_72h` | 최근 72시간 누적 강수량 |

이번 상관관계 확인 결과, 단일 시점 강수량보다 24시간, 48시간, 72시간 누적 강수량이 총방류량과 더 높은 관련성을 보였다. 따라서 XGBoost 학습 시 누적 강수량 변수는 중요한 후보 피처로 볼 수 있다.

## 7. 결측치 확인 및 처리 방향

병합 후 결측치는 다음과 같이 확인되었다.

| 컬럼명 | 결측 건수 |
|---|---:|
| `inflowqy` | 6 |
| `rain_lag_24h` | 7 |
| 그 외 컬럼 | 0 |

전체 29,339건 중 결측이 있는 행은 매우 적다. 1차 모델 학습에서는 다음 방식으로 처리하는 것이 적절하다.

- 목표값 `totdcwtrqy`가 없는 행은 제거
- 주요 입력 변수에 결측이 있는 행은 제거
- 제거 후 학습 가능 행 수는 약 29,333건

결측 행 수가 매우 적기 때문에 평균 대체나 보간보다 단순 제거가 더 안전하다. 다만 20개 댐 전체 모델로 확장할 때 결측 패턴이 댐별로 달라질 수 있으므로, 전체 데이터에서는 결측률을 다시 확인해야 한다.

## 8. 이상치 및 분포 확인

목표값인 총방류량 `totdcwtrqy`의 기초 통계는 다음과 같다.

| 항목 | 값 |
|---|---:|
| count | 29,339 |
| mean | 5.785 |
| std | 19.920 |
| min | 0.000 |
| 25% | 3.405 |
| 50% | 3.662 |
| 75% | 3.919 |
| max | 648.790 |

대부분의 시간대에서 방류량은 3~4 수준에 몰려 있지만, 최대값은 648.790으로 매우 크다. 이는 홍수기 또는 큰 강우 이벤트에서 방류량이 급격히 증가한 것으로 볼 수 있다.

XGBoost는 비선형 관계와 이상치에 비교적 강하지만, 목표값 분포가 치우쳐 있으므로 학습 단계에서는 다음을 추가로 검토하는 것이 좋다.

- 시간순 train/test 분리
- MAE, RMSE를 함께 확인
- 큰 방류 이벤트 구간에서 예측 성능 별도 확인
- 필요 시 `log1p(totdcwtrqy)` 변환 실험

## 9. 상관관계 분석 결과

총방류량 `totdcwtrqy`와 주요 변수의 상관관계는 다음과 같다.

| 변수 | 총방류량과 상관계수 |
|---|---:|
| `inflowqy` | 0.482 |
| `rain_sum_72h` | 0.379 |
| `rain_sum_48h` | 0.374 |
| `rain_sum_24h` | 0.285 |
| `rain_sum_12h` | 0.266 |
| `rain_sum_6h` | 0.241 |
| `rf` | 0.218 |
| `rain_sum_3h` | 0.208 |
| `rain_lag_6h` | 0.194 |
| `rain_lag_3h` | 0.188 |
| `rain_mm` | 0.181 |
| `rain_lag_1h` | 0.181 |
| `rain_lag_2h` | 0.181 |
| `rain_lag_24h` | 0.148 |
| `rain_lag_12h` | 0.140 |
| `rsvwtrt` | 0.094 |
| `rsvwtqy` | 0.094 |
| `lowlevel` | 0.094 |

상관관계만 보면 유입량 `inflowqy`가 가장 강한 변수이다. 강수량 변수 중에서는 현재 강수량보다 누적 강수량, 특히 48시간 및 72시간 누적 강수량이 더 높은 상관관계를 보였다.

이는 강수량이 바로 방류량으로 연결되기보다 유입량과 저수 상태를 거쳐 지연 반응을 보인다는 점과 일치한다.

## 10. XGBoost 학습 전 최종 입력 데이터 구성안

1차 XGBoost 모델에서는 다음과 같은 입력 변수를 후보로 사용할 수 있다.

### 10.1 운영 데이터 피처

- `inflowqy`
- `lowlevel`
- `rf`
- `rsvwtqy`
- `rsvwtrt`

### 10.2 강수량 피처

- `rain_mm`
- `rain_lag_1h`
- `rain_lag_3h`
- `rain_lag_6h`
- `rain_lag_12h`
- `rain_lag_24h`
- `rain_sum_6h`
- `rain_sum_12h`
- `rain_sum_24h`
- `rain_sum_48h`
- `rain_sum_72h`

### 10.3 목표값

- `totdcwtrqy`

## 11. 학습 데이터 분리 방식

댐 운영 데이터는 시간 순서가 중요한 시계열 데이터이다. 따라서 일반적인 랜덤 분할은 권장하지 않는다.

권장 방식은 다음과 같다.

1. 데이터를 `obsrdt` 기준으로 정렬한다.
2. 앞쪽 70~80% 기간을 학습 데이터로 사용한다.
3. 뒤쪽 20~30% 기간을 테스트 데이터로 사용한다.
4. 필요하면 학습 데이터 내부에서 마지막 일부를 검증 데이터로 분리한다.

이렇게 해야 미래 데이터를 과거 학습에 사용하는 데이터 누수를 방지할 수 있다.

## 12. 현재 단계 결론

보령댐 기준으로 운영 데이터와 강수량 데이터는 시간 기준으로 정상 병합되었다.

특히 다음 점에서 XGBoost 방류량 예측 실험을 진행할 수 있다고 판단된다.

- 운영 데이터와 강수량 데이터의 시간 매칭률이 100%
- 강수량 결측이 없음
- 학습 가능 행 수가 약 29,333건으로 충분함
- 강수량 누적 변수와 총방류량 사이에 의미 있는 양의 상관관계가 확인됨
- 단일 강수량보다 누적 강수량이 더 유용한 피처로 보임

따라서 다음 단계에서는 보령댐 단일 모델을 먼저 학습하고, 이후 동일한 전처리 방식을 20개 다목적댐 전체로 확장하는 것이 적절하다.

## 13. 생성된 중간 산출물

현재 프로젝트 폴더에는 다음 중간 산출물이 생성되어 있다.

| 파일명 | 설명 |
|---|---|
| `보령댐_운영데이터.csv` | DB에서 추출한 보령댐 운영 데이터 |
| `보령댐_운영_강수량_병합데이터.csv` | 운영 데이터와 강수량 파생 변수를 병합한 학습 후보 데이터 |
| `보령댐_전처리보고서.md` | 본 전처리 보고서 |
