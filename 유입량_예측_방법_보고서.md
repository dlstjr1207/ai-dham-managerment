# 유입량 예측 방법 보고서

## 1. 목적

이 보고서는 남강댐, 임하댐처럼 특정 댐 하나를 선택한 뒤 해당 댐의 과거 운영자료와 강수량 자료를 이용해 3시간 뒤 유입량을 예측한 방법을 정리한 문서이다.  
전체 댐을 하나의 모델로 묶어 학습하지 않고, 댐별 특성이 다르다고 보고 각 댐마다 개별 모델을 따로 학습하는 방식을 기준으로 한다.

## 2. 사용 데이터

사용한 최종 데이터 파일은 다음과 같다.

```text
final_data_20_weather.csv
```

이 파일에는 20개 다목적댐의 시간별 운영자료와 강수량 자료가 들어 있다. 유입량 예측에는 이 중에서 학습할 댐의 자료만 필터링해서 사용한다.

예를 들어 남강댐 모델은 남강댐 자료만 사용하고, 임하댐 모델은 임하댐 자료만 사용한다.

| 댐 | 댐 코드 | 학습 방식 |
|---|---:|---|
| 남강댐 | 2018110 | 남강댐 데이터만 사용 |
| 임하댐 | 2002110 | 임하댐 데이터만 사용 |

## 3. 예측 목표

예측 목표는 현재 시점의 자료를 이용해 3시간 뒤 유입량을 예측하는 것이다.

```text
target_inflow_3h(t) = inflowqy(t + 3)
```

즉, 현재 시점 `t`에서 관측 가능한 유입량, 과거 유입량, 강수량, 저수위, 저수량, 저수율 등을 이용해 3시간 뒤의 유입량 `inflowqy(t+3)`을 예측했다.

## 4. 전처리 과정

전처리는 다음 순서로 진행했다.

1. `obsrdt` 컬럼을 시간 자료로 변환했다.
2. 학습할 댐 코드만 필터링했다.
3. 시간 순서대로 정렬했다.
4. 과거 시점 값을 이용한 지연 변수를 만들었다.
5. 최근 몇 시간의 평균 유입량과 누적 강수량을 만들었다.
6. 3시간 뒤 유입량을 예측 목표값으로 만들었다.
7. 학습에 필요한 입력값이나 목표값이 비어 있는 행은 제거했다.

임의의 값은 넣지 않았다. 결측치를 평균값, 0, 임의 숫자로 채워 넣지 않았고, 모델 학습에 필요한 값이 없는 행은 제외했다.

## 5. 사용한 입력 변수

유입량 예측에 사용한 주요 입력 변수는 다음과 같다.

| 구분 | 변수 |
|---|---|
| 현재 유입량 | `inflowqy` |
| 과거 유입량 | `inflow_lag_1h`, `inflow_lag_3h`, `inflow_lag_6h`, `inflow_lag_12h` |
| 평균 유입량 | `inflow_mean_3h`, `inflow_mean_6h`, `inflow_mean_12h`, `inflow_mean_24h` |
| 현재 강수량 | `rain` |
| 과거 강수량 | `rain_lag_1h`, `rain_lag_3h`, `rain_lag_6h`, `rain_lag_12h` |
| 누적 강수량 | `rain_sum_3h`, `rain_sum_6h`, `rain_sum_12h`, `rain_sum_24h`, `rain_sum_48h`, `rain_sum_72h` |
| 댐 상태 | `lowlevel`, `rsvwtqy`, `rsvwtrt` |

## 6. 파생 변수 계산식

과거 유입량은 현재 시점보다 이전의 유입량을 가져와 만들었다.

```text
inflow_lag_3h(t) = inflowqy(t - 3)
```

최근 평균 유입량은 일정 시간 동안의 유입량 평균으로 계산했다.

```text
inflow_mean_6h(t)
= [inflowqy(t) + inflowqy(t-1) + ... + inflowqy(t-5)] / 6
```

누적 강수량은 일정 시간 동안의 강수량 합으로 계산했다.

```text
rain_sum_24h(t)
= rain(t) + rain(t-1) + ... + rain(t-23)
```

최종 유입량 예측식은 다음과 같이 표현할 수 있다.

```text
inflowqy(t+3)
= f_inflow(
    현재 유입량,
    과거 유입량,
    평균 유입량,
    현재 강수량,
    과거 강수량,
    누적 강수량,
    저수위,
    저수량,
    저수율
  )
```

여기서 `f_inflow`는 학습된 머신러닝 모델이다.

## 7. 학습 및 평가 방법

데이터는 시간 순서를 유지해서 나누었다. 미래 자료가 과거 학습에 섞이지 않도록 무작위 분할은 사용하지 않았다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | 2023년 1월 1일 ~ 2024년 12월 31일 |
| 평가 데이터 | 2025년 1월 1일 ~ 2025년 12월 31일 |

모델은 `GradientBoostingRegressor`와 `RandomForestRegressor`를 비교했다. 평가 지표는 MAE, RMSE, R2를 사용했다.

비교 기준선은 “현재 유입량이 3시간 뒤에도 그대로 유지된다”는 단순 기준선을 사용했다.

```text
baseline_prediction(t+3) = inflowqy(t)
```

이 기준선보다 모델의 MAE와 RMSE가 낮고 R2가 높으면, 모델이 단순 유지 예측보다 더 잘 예측한 것으로 해석했다.

## 8. 현재 실험 결과 요약

| 댐 | 모델 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---:|---:|---:|---:|
| 남강댐 | GradientBoosting | 23.044 | 14.733 | 0.657 | 0.814 |
| 남강댐 | RandomForest | 23.044 | 14.812 | 0.657 | 0.810 |
| 임하댐 | GradientBoosting | 9.149 | 8.096 | 0.777 | 0.841 |
| 임하댐 | RandomForest | 9.149 | 8.861 | 0.777 | 0.837 |

현재 결과에서는 두 댐 모두 `GradientBoostingRegressor`가 가장 좋은 결과를 보였다.  
남강댐은 기준선보다 성능 개선 폭이 컸고, 임하댐은 전체적으로 R2가 높게 나왔다.

## 9. 팀원이 실행할 때 확인할 점

팀원이 같은 방식으로 다른 댐을 실험할 때는 전체 댐을 한 번에 학습하지 말고, 먼저 댐 코드를 하나 선택해서 해당 댐 자료만 분리한 뒤 같은 전처리와 학습 과정을 적용하면 된다.

필요한 실행 파일은 다음과 같다.

```text
남강댐_임하댐_개별학습_유입량_방류량.py
```

이 파일에서 댐 코드와 댐 이름을 바꾸면 다른 댐도 같은 방식으로 개별 학습할 수 있다.
