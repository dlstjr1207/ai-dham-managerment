# 남강댐/임하댐 개별 학습 유입량 및 방류량 예측 상세 보고서

## 1. 실험 목적

전체 20개 다목적댐 통합 실험에서 남강댐과 임하댐의 유입량 예측 성능이 상대적으로 좋게 나타났다. 따라서 이번 실험에서는 두 댐을 하나의 통합 모델에 넣지 않고, 각각 따로 분리하여 개별 모델을 학습하였다.

실험 목표는 다음과 같다.

1. 남강댐 단독 유입량 예측
2. 임하댐 단독 유입량 예측
3. 남강댐 단독 방류량 예측
4. 임하댐 단독 방류량 예측
5. 전처리 공식과 학습 방법을 명확히 기록

## 2. 사용 파일

사용한 원천 파일은 다음과 같다.

```text
final_data_20_weather.csv
```

이 파일은 팀원이 전달한 `final_data.csv`에서 20개 다목적댐만 필터링한 데이터이다. DB 기준으로는 다음 테이블과 같은 내용이다.

```text
public.dam_operation_data_weather
```

이번 실험 스크립트는 다음 파일이다.

```text
남강댐_임하댐_개별학습_유입량_방류량.py
```

## 3. 대상 댐

| 댐 이름 | 댐 코드 |
|---|---:|
| 남강댐 | 2018110 |
| 임하댐 | 2002110 |

두 댐은 각각 따로 필터링하여 학습하였다. 즉 남강댐 모델은 남강댐 데이터만 사용했고, 임하댐 모델은 임하댐 데이터만 사용했다.

## 4. 학습/평가 기간

시간순 분할을 사용하였다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | 2023년 1월 1일 ~ 2024년 12월 31일 |
| 평가 데이터 | 2025년 1월 1일 ~ 2025년 12월 31일 |

무작위 분할은 사용하지 않았다. 시계열 예측에서 무작위 분할을 사용하면 미래 데이터가 학습에 섞이는 문제가 생길 수 있기 때문이다.

## 5. 결측치 처리

임의값 대체는 사용하지 않았다.

사용하지 않은 방식은 다음과 같다.

- 평균값 대체
- 중앙값 대체
- 0 대체
- 임의 보간
- 임의 유입량 생성
- 임의 방류량 생성

시차 변수와 이동 평균/누적 변수를 만든 뒤, 입력 변수 또는 목표값에 결측이 있는 행은 제거하였다.

## 6. 유입량 예측 공식

유입량 예측 목표는 3시간 뒤 유입량이다.

```text
target_inflow_3h(t) = inflowqy(t+3)
```

모델식은 다음과 같이 표현할 수 있다.

```text
inflowqy(t+3)
= f_inflow(
    inflowqy(t),
    inflowqy(t-1),
    inflowqy(t-3),
    inflowqy(t-6),
    inflowqy(t-12),
    mean_inflow_3h(t),
    mean_inflow_6h(t),
    mean_inflow_12h(t),
    mean_inflow_24h(t),
    rain(t),
    rain(t-1),
    rain(t-3),
    rain(t-6),
    rain(t-12),
    sum_rain_3h(t),
    sum_rain_6h(t),
    sum_rain_12h(t),
    sum_rain_24h(t),
    sum_rain_48h(t),
    sum_rain_72h(t),
    lowlevel(t),
    rsvwtqy(t),
    rsvwtrt(t)
  )
```

여기서 `f_inflow`는 학습된 머신러닝 모델이다.

## 7. 방류량 예측 공식

방류량 예측 목표는 3시간 뒤 총방류량이다.

```text
target_discharge_3h(t) = totdcwtrqy(t+3)
```

기본 방류량 예측식은 다음과 같다.

```text
totdcwtrqy(t+3)
= f_discharge(
    totdcwtrqy(t),
    totdcwtrqy(t-1),
    totdcwtrqy(t-3),
    totdcwtrqy(t-6),
    totdcwtrqy(t-12),
    mean_discharge_3h(t),
    mean_discharge_6h(t),
    mean_discharge_12h(t),
    inflowqy(t),
    inflowqy(t-1),
    inflowqy(t-3),
    inflowqy(t-6),
    mean_inflow_3h(t),
    mean_inflow_6h(t),
    rain(t),
    rain(t-3),
    rain(t-6),
    sum_rain_3h(t),
    sum_rain_6h(t),
    sum_rain_12h(t),
    sum_rain_24h(t),
    sum_rain_48h(t),
    sum_rain_72h(t),
    lowlevel(t),
    rsvwtqy(t),
    rsvwtrt(t)
  )
```

참고용 실험에서는 실제 3시간 뒤 유입량도 추가하였다.

```text
totdcwtrqy(t+3)
= f_discharge(..., inflowqy(t+3))
```

단, `inflowqy(t+3)`는 실제 예측 시점에서는 알 수 없는 값이므로 운영용 입력이 아니라 참고용 상한선으로만 해석해야 한다.

## 8. 파생 변수 계산 공식

### 8.1 시차 변수

```text
inflow_lag_3h(t) = inflowqy(t-3)
rain_lag_6h(t) = rain(t-6)
discharge_lag_12h(t) = totdcwtrqy(t-12)
```

### 8.2 평균 유입량

```text
mean_inflow_3h(t)
= [inflowqy(t) + inflowqy(t-1) + inflowqy(t-2)] / 3
```

```text
mean_inflow_6h(t)
= [inflowqy(t) + inflowqy(t-1) + ... + inflowqy(t-5)] / 6
```

### 8.3 누적 강수량

```text
sum_rain_6h(t)
= rain(t) + rain(t-1) + rain(t-2) + rain(t-3) + rain(t-4) + rain(t-5)
```

```text
sum_rain_24h(t)
= rain(t) + rain(t-1) + ... + rain(t-23)
```

### 8.4 평균 방류량

```text
mean_discharge_3h(t)
= [totdcwtrqy(t) + totdcwtrqy(t-1) + totdcwtrqy(t-2)] / 3
```

## 9. 사용 모델

이번 개별 학습에서는 다음 두 모델을 사용하였다.

| 모델 | 사용 이유 |
|---|---|
| GradientBoostingRegressor | 이전 실험에서 일반 구간 성능이 안정적이었음 |
| RandomForestRegressor | 유입량 급증 이벤트에서 강한 성능을 보였음 |

XGBoost는 전체 실험에서 후보로 사용했지만, 이번 개별 상세 보고서에서는 실행 시간과 비교 안정성을 고려하여 위 두 모델 중심으로 재학습하였다.

## 10. 남강댐 유입량 예측 결과

| 모델 | 학습 행 수 | 평가 행 수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---:|---:|---:|---:|---:|---:|
| GradientBoosting | 17,050 | 8,596 | 23.044 | 14.733 | 0.657 | 0.814 |
| RandomForest | 17,050 | 8,596 | 23.044 | 14.812 | 0.657 | 0.810 |

남강댐 유입량 예측은 두 모델 모두 기준선보다 크게 좋아졌다.

가장 좋은 모델은 GradientBoosting이다.

```text
MAE 개선: 23.044 → 14.733
R2 개선: 0.657 → 0.814
```

## 11. 임하댐 유입량 예측 결과

| 모델 | 학습 행 수 | 평가 행 수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---:|---:|---:|---:|---:|---:|
| GradientBoosting | 17,303 | 8,760 | 9.149 | 8.096 | 0.777 | 0.841 |
| RandomForest | 17,303 | 8,760 | 9.149 | 8.861 | 0.777 | 0.837 |

임하댐도 유입량 예측에서 기준선보다 성능이 좋아졌다.

가장 좋은 모델은 GradientBoosting이다.

```text
MAE 개선: 9.149 → 8.096
R2 개선: 0.777 → 0.841
```

## 12. 남강댐 방류량 예측 결과

| 입력 조합 | 모델 | 학습 행 수 | 평가 행 수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---:|---:|---:|---:|---:|---:|
| 기본피처 | GradientBoosting | 17,126 | 8,623 | 10.979 | 13.651 | 0.889 | 0.896 |
| 실제미래유입량 참고 | GradientBoosting | 17,126 | 8,623 | 10.979 | 13.601 | 0.889 | 0.897 |
| 기본피처 | RandomForest | 17,126 | 8,623 | 10.979 | 14.357 | 0.889 | 0.850 |
| 실제미래유입량 참고 | RandomForest | 17,126 | 8,623 | 10.979 | 14.355 | 0.889 | 0.848 |

남강댐 방류량은 R2 기준으로 GradientBoosting이 기준선보다 약간 높다.

다만 MAE 기준으로는 현재 방류량 유지 기준선이 더 좋다. 즉 방류량 자체는 짧은 시간 동안 유지되는 성질이 강하다.

## 13. 임하댐 방류량 예측 결과

| 입력 조합 | 모델 | 학습 행 수 | 평가 행 수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---:|---:|---:|---:|---:|---:|
| 기본피처 | GradientBoosting | 17,388 | 8,760 | 9.903 | 10.380 | -0.657 | 0.099 |
| 실제미래유입량 참고 | GradientBoosting | 17,388 | 8,760 | 9.903 | 10.700 | -0.657 | 0.138 |
| 기본피처 | RandomForest | 17,388 | 8,760 | 9.903 | 10.742 | -0.657 | -0.091 |
| 실제미래유입량 참고 | RandomForest | 17,388 | 8,760 | 9.903 | 10.323 | -0.657 | 0.034 |

임하댐 방류량은 R2 기준으로는 기준선보다 개선되었다. 특히 기준선 R2가 음수였기 때문에 모델이 분산 설명력은 더 낫다.

하지만 MAE 기준으로는 기준선보다 조금 나쁘다.

## 14. 최종 해석

이번 개별 학습의 핵심 결론은 다음과 같다.

1. 남강댐과 임하댐 모두 유입량 예측은 기준선보다 확실히 좋아졌다.
2. 두 댐 모두 유입량 예측에서는 GradientBoosting이 가장 좋았다.
3. 남강댐 유입량 예측은 특히 성능 개선이 크다.
4. 방류량 예측은 유입량 예측보다 어렵다.
5. 방류량은 현재 방류량 유지 기준선이 MAE에서 매우 강하다.
6. 실제 미래 유입량을 참고용으로 넣어도 방류량 성능이 크게 좋아지지는 않았다.

따라서 프로젝트의 핵심 성과는 다음처럼 잡는 것이 가장 설득력 있다.

```text
강수량과 운영자료를 이용한 3시간 뒤 유입량 예측은 가능하며,
남강댐과 임하댐에서 기준선보다 개선된 성능을 확인했다.
방류량 예측은 운영 정책 영향이 커서 추가 모델링이 필요하다.
```

## 15. 생성 산출물

| 파일명 | 설명 |
|---|---|
| `남강댐_임하댐_개별학습_유입량_방류량.py` | 개별 댐 학습 스크립트 |
| `남강댐_임하댐_유입량예측_평가결과.csv` | 유입량 예측 평가 결과 |
| `남강댐_임하댐_방류량예측_평가결과.csv` | 방류량 예측 평가 결과 |
| `남강댐_임하댐_유입량예측_예측결과.csv` | 유입량 예측값 |
| `남강댐_임하댐_방류량예측_예측결과.csv` | 방류량 예측값 |
| `남강댐_임하댐_개별학습_상세보고서.md` | 본 보고서 |

