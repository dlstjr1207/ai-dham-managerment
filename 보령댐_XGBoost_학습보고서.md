# 보령댐 XGBoost 1시간 뒤 방류량 예측 학습 보고서

## 1. 실험 목적

본 실험은 20개 다목적댐 전체 모델링에 들어가기 전에, 보령댐 단일 데이터를 대상으로 XGBoost를 사용해 방류량 예측이 가능한지 확인하기 위한 1차 실험이다.

이번 단계에서는 임의값을 생성하거나 가짜 데이터를 추가하지 않았다. 사용한 데이터는 다음 두 자료를 시간 기준으로 병합한 실제 관측 및 예보 기반 자료이다.

- `aidam` 데이터베이스의 보령댐 운영 데이터
- 사용자가 제공한 보령댐 1시간 강수량 CSV

## 2. 사용 데이터

학습에는 다음 병합 파일을 사용하였다.

```text
보령댐_운영_강수량_병합데이터.csv
```

이 파일은 보령댐 운영 데이터와 강수량 파생 변수를 `obsrdt` 기준으로 병합한 데이터이다.

- 전체 행 수: 29,339건
- 시간 범위: `2023-01-01 01:00:00` ~ `2026-05-07 11:00:00`
- 시간 중복: 0건
- 강수량 매칭 누락: 0건

## 3. 예측 목표값 설정

이번 실험에서는 현재 시각의 정보로 **다음 1시간 뒤 총방류량**을 예측하도록 설정하였다.

원래 총방류량 컬럼은 다음과 같다.

```text
totdcwtrqy
```

학습용 목표값은 아래 방식으로 생성하였다.

```text
target_totdcwtrqy_1h = 다음 시각의 totdcwtrqy
```

즉, `2023-01-01 01:00:00`의 입력 변수로 `2023-01-01 02:00:00`의 총방류량을 예측한다.

이 방식은 현재 시각의 자료로 미래 시각의 방류량을 예측하는 형태이므로, 같은 시각의 방류량을 단순히 맞추는 설명 모델보다 예측 문제에 더 가깝다.

## 4. 전처리 방식

### 4.1 시간 정렬

데이터는 `obsrdt` 기준으로 오름차순 정렬하였다.

시계열 데이터이기 때문에 학습과 테스트를 무작위로 섞지 않았다. 무작위 분할을 사용하면 미래 데이터가 학습 과정에 섞이는 데이터 누수가 발생할 수 있기 때문이다.

### 4.2 결측치 처리

임의값 대체는 사용하지 않았다.

다음 방식만 사용하였다.

- 목표값이 없는 행 제거
- 입력 변수 중 결측이 있는 행 제거

결측 제거 후 행 수는 실험에 사용한 피처 조합에 따라 달라진다.

| 실험 | 원본 행 수 | 결측 제거 후 행 수 | 제거 행 수 |
|---|---:|---:|---:|
| 운영 데이터만 | 29,339 | 29,332 | 7 |
| 강수량만 | 29,339 | 29,331 | 8 |
| 운영 데이터 + 강수량 | 29,339 | 29,325 | 14 |
| 운영 데이터 + 현재 방류량 | 29,339 | 29,332 | 7 |
| 운영 데이터 + 강수량 + 현재 방류량 | 29,339 | 29,325 | 14 |

결측치를 평균, 중앙값, 0 등으로 채우지 않았다.

### 4.3 학습 및 테스트 기간 분리

시간 기준으로 다음과 같이 분리하였다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | `2023-01-01` ~ `2025-12-31` |
| 테스트 데이터 | `2026-01-01` ~ `2026-05-07 10:00:00` |

테스트 시작일을 `2026-01-01`로 둔 이유는, 2023~2025년 전체를 과거 학습 기간으로 사용하고 2026년을 미래 검증 기간처럼 보기 위해서이다.

## 5. 사용한 피처 조합

총 5가지 피처 조합을 비교하였다.

### 5.1 운영 데이터만

```text
inflowqy
lowlevel
rf
rsvwtqy
rsvwtrt
```

### 5.2 강수량만

```text
rain_mm
rain_lag_1h
rain_lag_2h
rain_lag_3h
rain_lag_6h
rain_lag_12h
rain_lag_24h
rain_sum_3h
rain_sum_6h
rain_sum_12h
rain_sum_24h
rain_sum_48h
rain_sum_72h
```

### 5.3 운영 데이터 + 강수량

운영 데이터 피처와 강수량 피처를 함께 사용하였다.

### 5.4 운영 데이터 + 현재 방류량

다음 1시간 뒤 방류량을 예측할 때, 현재 시각의 방류량 `totdcwtrqy`는 실제 운영 상황에서 사용 가능한 관측값이다.

따라서 현재 방류량을 자기회귀 피처로 추가한 실험도 수행하였다.

### 5.5 운영 데이터 + 강수량 + 현재 방류량

가장 많은 정보를 사용한 조합이다.

```text
운영 데이터 피처 + 강수량 피처 + 현재 시각 총방류량
```

## 6. 모델 설정

모델은 `xgboost.XGBRegressor`를 사용하였다.

이번 실험은 1차 가능성 확인이 목적이므로 하이퍼파라미터 튜닝은 수행하지 않았다. 모델에는 회귀 목적 함수만 명시하였다.

```text
objective = reg:squarederror
```

그 외 설정은 XGBoost 기본값을 사용하였다.

## 7. 평가 지표

평가 지표는 다음 3가지를 사용하였다.

| 지표 | 의미 |
|---|---|
| MAE | 실제값과 예측값의 절대 오차 평균 |
| RMSE | 큰 오차에 더 민감한 평균 제곱근 오차 |
| R2 | 테스트 데이터 분산 대비 설명력 |

이번 테스트 구간의 목표값 분포는 다음과 같다.

- 테스트 목표값 평균: 약 3.515
- 테스트 목표값 표준편차: 약 0.493

즉, 2026년 테스트 구간에서는 방류량 변동 폭이 크지 않았다. 이 때문에 R2는 작은 오차 차이에도 민감하게 나빠질 수 있다.

## 8. 학습 결과

| 실험 | MAE | RMSE | R2 |
|---|---:|---:|---:|
| 운영 데이터만 | 0.447 | 0.761 | -1.379 |
| 강수량만 | 0.616 | 1.256 | -5.479 |
| 운영 데이터 + 강수량 | 0.362 | 1.067 | -3.676 |
| 운영 데이터 + 현재 방류량 | 0.162 | 0.386 | 0.387 |
| 운영 데이터 + 강수량 + 현재 방류량 | 0.154 | 0.343 | 0.515 |

결과를 보면, 현재 방류량을 넣지 않은 모델에서는 R2가 음수로 나타났다. 이는 모델이 테스트 구간의 평균 예측보다도 분산 설명 측면에서 좋지 않았다는 의미이다.

반면 현재 시각의 방류량을 입력 변수로 추가하면 성능이 크게 개선되었다.

특히 `운영 데이터 + 강수량 + 현재 방류량` 조합이 XGBoost 모델 중 가장 좋은 결과를 보였다.

## 9. 기준선 모델과 비교

이번 실험에서는 단순 기준선도 함께 확인하였다.

### 9.1 학습 기간 평균 기준선

학습 데이터의 평균 방류량을 모든 테스트 시각에 그대로 예측하는 방식이다.

- MAE: 약 2.47

이 기준선은 XGBoost 모델보다 훨씬 나쁘다.

### 9.2 현재 방류량 유지 기준선

현재 시각의 총방류량을 다음 1시간 뒤 예측값으로 그대로 사용하는 방식이다.

```text
prediction(t+1) = totdcwtrqy(t)
```

결과는 다음과 같다.

| 기준선 | MAE | RMSE | R2 |
|---|---:|---:|---:|
| 현재 방류량 유지 | 0.117 | 0.217 | 0.806 |

이 기준선은 모든 XGBoost 실험보다 좋았다.

이는 보령댐의 2026년 테스트 구간에서 방류량이 시간적으로 매우 안정적이었고, 직전 시간의 방류량이 다음 시간 방류량을 강하게 설명한다는 뜻이다.

따라서 현재 결과만 놓고 보면, XGBoost가 단순 지속성 기준선을 아직 이기지 못했다.

## 10. 변수 중요도

### 10.1 운영 데이터만 사용한 모델

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `lowlevel` | 0.348 |
| 2 | `inflowqy` | 0.256 |
| 3 | `rsvwtqy` | 0.149 |
| 4 | `rf` | 0.129 |
| 5 | `rsvwtrt` | 0.119 |

### 10.2 강수량만 사용한 모델

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `rain_sum_48h` | 0.247 |
| 2 | `rain_sum_12h` | 0.138 |
| 3 | `rain_sum_72h` | 0.128 |
| 4 | `rain_sum_6h` | 0.119 |
| 5 | `rain_sum_24h` | 0.115 |

강수량만 사용할 때도 현재 강수량 `rain_mm`보다 누적 강수량 변수가 더 중요하게 나타났다.

### 10.3 운영 데이터 + 강수량 + 현재 방류량 모델

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `rsvwtrt` | 0.503 |
| 2 | `totdcwtrqy` | 0.303 |
| 3 | `rain_sum_6h` | 0.059 |
| 4 | `rain_lag_12h` | 0.055 |
| 5 | `rain_lag_24h` | 0.026 |
| 6 | `rain_sum_12h` | 0.016 |
| 7 | `lowlevel` | 0.015 |
| 8 | `rsvwtqy` | 0.007 |

강수량 변수 중에서는 `rain_sum_6h`, `rain_lag_12h`, `rain_lag_24h` 등이 상위에 나타났다.

## 11. 해석

이번 결과에서 가장 중요한 점은 다음과 같다.

1. 강수량 데이터는 운영 데이터와 정상적으로 연결된다.
2. 강수량을 추가하면 XGBoost 모델의 MAE가 일부 개선된다.
3. 현재 방류량을 입력 변수로 넣으면 모델 성능이 크게 좋아진다.
4. 하지만 현재 방류량 유지 기준선이 아직 XGBoost보다 더 좋다.

따라서 지금 단계에서 바로 “XGBoost 모델이 충분히 좋다”고 말하기는 어렵다.

다만 이는 강수량 데이터가 쓸모없다는 뜻은 아니다. 보령댐의 2026년 테스트 구간은 방류량 변동이 작았고, 이런 구간에서는 단순히 현재 방류량을 유지하는 예측이 매우 강하게 작동한다.

실제로 큰 강우 이벤트나 급격한 방류 변화가 있는 구간에서는 누적 강수량과 유입량 정보가 더 중요해질 가능성이 높다.

## 12. 다음 단계 제안

다음 단계에서는 아래 방향으로 개선하는 것이 좋다.

1. 보령댐에서 예측 시점을 1시간 뒤뿐 아니라 3시간, 6시간 뒤로 확장한다.
2. 큰 방류 이벤트가 있는 구간만 따로 평가한다.
3. 현재 방류량 유지 기준선을 반드시 함께 비교한다.
4. 하이퍼파라미터 튜닝 전, 데이터 분할 방식을 먼저 검토한다.
5. 20개 댐 전체로 확장할 때는 댐별 성능을 따로 확인한다.

## 13. 생성된 산출물

이번 실험으로 생성된 파일은 다음과 같다.

| 파일명 | 설명 |
|---|---|
| `보령댐_XGBoost_학습.py` | 보령댐 XGBoost 학습 스크립트 |
| `보령댐_XGBoost_평가결과.csv` | 피처 조합별 평가 결과 |
| `보령댐_XGBoost_예측결과.csv` | 테스트 구간 예측 결과 |
| `보령댐_XGBoost_피처중요도.csv` | 피처 중요도 결과 |
| `보령댐_XGBoost_학습보고서.md` | 본 학습 보고서 |
