# AI 기반 댐 방류량 예측 시스템 (Phase 2) 기술 보고서

## 1. 프로젝트 개요 및 목표
본 프로젝트는 기후 변화로 인한 국지성 호우 증가 및 기상 이변에 선제적으로 대응하기 위해 기획되었습니다. 공공데이터포털(data.go.kr)에서 제공하는 기상청 날씨 예보 데이터와 한국수자원공사의 댐 수문 운영 데이터를 실시간으로 연동하여, **기상 상황에 따른 댐의 유입량을 예측하고 최적의 방류량 및 방류 시점을 도출하는 의사결정 지원 시스템(Decision Support System)**을 구축하는 것을 목표로 합니다.

---

## 2. 데이터 수집 및 연동 아키텍처 (API 6종 융합)

본 시스템은 신뢰도 높은 국가 공공데이터 API 6종을 상호 교차 연동(Mashup)하여 파이프라인을 구축했습니다.

### 2.1 한국수자원공사 (댐 제원 및 수문 현황)
1. **댐코드 조회 API**: 전국의 다목적댐 및 용수댐 리스트와 고유 식별 코드(`damCd`)를 동적으로 파싱하여 사용자 맞춤형 지역(댐) 선택 기능을 제공합니다.
2. **다목적댐 운영 정보 API**: 선택된 댐의 스펙(홍수기 제한수위, 총 저수용량 등)을 초기화하여 방류 임계점 계산에 활용합니다.
3. **용수댐 운영 정보 API**: 다목적댐 외에 지역별 핵심 용수댐의 스펙을 보완합니다.
4. **수문 운영 정보 (시간단위) API**: 선택된 댐의 최근 24시간 실시간 유입량, 현재 저수위, 방류량, 강우량 데이터를 수집하여 현재 댐의 '초기 조건(Initial Condition)'을 정의합니다.

### 2.2 기상청 (미래 강수 예측)
수자원공사 데이터만으로는 '과거~현재'의 수위만 알 수 있으므로, 기상청 API를 결합하여 '미래'를 예측합니다.
5. **단기예보 조회서비스**: 각 댐의 위경도 좌표를 기상청 격자 좌표(X, Y)로 변환한 후, 향후 3일간(72시간)의 시간당 강수량 예보 데이터를 수집합니다.
6. **중기예보 조회서비스**: 3일 이후~10일까지의 강수 확률 및 기상 전망을 수집하여 장기적인 수위 관리 시나리오에 참고 지표로 사용합니다.

---

## 3. 핵심 알고리즘 및 예측 모델 (Machine Learning)

데이터 파이프라인을 통해 수집된 데이터는 아래와 같은 수치해석 및 머신러닝 파이프라인을 거쳐 최적 방류량을 도출합니다.

### 3.1 LSTM (Long Short-Term Memory) 기반 유입량 예측 모델
단순히 "비가 오면 물이 들어온다"는 1차원적 계산을 넘어, 시간의 흐름을 기억하는 순환신경망(RNN) 계열의 **LSTM 모델**을 설계합니다.
- **Input Feature**: 기상청 시간당 예측 강수량, 과거 24시간 실제 유입량 흐름, 댐 유역 면적 지수.
- **Output**: 향후 72시간 동안 시간대별 댐으로 들어올 **예상 유입량(m³/s) 곡선**.
- **역할**: 강우가 강 하구를 타고 댐으로 모이기까지의 '지연 시간(Time Lag)'과 토양의 수분 흡수 한계를 학습하여 매우 정확한 유입량 시계열 데이터를 예측합니다.

### 3.2 수문학적 방류 임계점 수치해석 (Numerical Method)
LSTM을 통해 미래 72시간의 총 유입량이 계산되면, 이를 댐의 표면적 및 현재 수위 변수와 결합하여 **예상 최고 수위**를 산출합니다.
- 예측 수위 산출식: `현재 수위 + (예상 총 유입량 / 댐 표면적)`
- 이 예측 수위가 수자원공사 API에서 받아온 **홍수기 제한수위(Limit Water Level)**를 초과하는지 모니터링합니다.

### 3.3 XGBoost (Extreme Gradient Boosting) 기반 최적 방류량 의사결정
수위 초과 위험이 감지되면, 하류 피해를 최소화하면서 댐의 안전을 지킬 수 있는 **최적 방류량**을 도출합니다.
- **왜 XGBoost인가?**: 앙상블(Ensemble) 기법 기반의 의사결정 트리 모델로, 수많은 환경 변수(비가 오는 속도, 현재 수위의 여유분, 하류 하천의 한계 수위치 등)를 다각도로 평가하여 가장 페널티가 적은 방류 수치를 뽑아내는데 탁월합니다.
- **적용 방식**: 
  위험 감지 시, XGBoost 모델은 (1) 댐이 범람하지 않기 위한 필요 방류량, (2) 급격한 방류로 인한 하류 범람 확률을 스코어링하여 **'시간당 X톤씩 Y시간 동안 점진적 방류'** 형태의 안전 시나리오를 산출 및 권고합니다.

---

## 4. 시각화 및 시스템 UI (Streamlit 프레임워크)
위의 모든 데이터 파이프라인과 예측 알고리즘은 **Python Streamlit** 프레임워크를 통해 실시간 웹 대시보드로 시각화됩니다.
- **이중 축(Dual-axis) 동적 그래프**: Plotly 라이브러리를 통해 상단에는 바(Bar) 차트 형태의 강수량을, 하단에는 꺾은선(Line) 형태의 유입량 및 저수위 변화를 직관적으로 겹쳐서 렌더링합니다.
- **실시간 Alert 시스템**: 머신러닝 예측 결과 제한수위 도달이 예상될 시 즉각적으로 붉은색 경고 위젯과 함께 권고 방류량을 화면에 표출하여 관리자의 신속한 판단을 돕습니다.

## 5. 결론 및 기대효과
본 시스템은 파편화된 공공데이터(기상청, 수자원공사)를 통합된 하나의 머신러닝 파이프라인(LSTM + XGBoost)으로 묶어낸 훌륭한 AI 활용 사례입니다. 향후 국토교통부의 지형/하천 API까지 추가 연동된다면, 하류 지역의 침수 위험 분류 등급까지 산출할 수 있는 국가 단위 재난 방재 솔루션으로의 확장이 기대됩니다.
