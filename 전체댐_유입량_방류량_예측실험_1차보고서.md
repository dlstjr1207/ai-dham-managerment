# 전체 20개 다목적댐 유입량 및 방류량 예측 1차 실험 보고서

## 1. 실험 목적

팀원이 전달한 `final_data.csv`를 검증하고 DB에 적재한 뒤, 20개 다목적댐 전체를 대상으로 다음 2단계 예측 실험을 수행하였다.

```text
1단계: 강수량 + 과거 유입량 + 댐 상태 → 3시간 뒤 유입량 예측
2단계: 예측 유입량 + 방류량/유입량/강수량/댐 상태 → 3시간 뒤 방류량 예측
```

이번 실험은 전체 20개 댐으로 확장했을 때 모델이 돌아가는지, 그리고 보령댐 단일 실험보다 데이터가 늘었을 때 성능 흐름이 달라지는지 확인하기 위한 1차 실험이다.

## 2. 사용 데이터

사용 데이터는 `public.dam_operation_data_weather`에 적재한 20개 다목적댐 강수량 포함 데이터와 동일한 CSV이다.

```text
final_data_20_weather.csv
```

데이터 개요는 다음과 같다.

| 항목 | 값 |
|---|---:|
| 댐 수 | 20 |
| 전체 행 수 | 586,780 |
| 시간 범위 | 2023-01-01 01:00:00 ~ 2026-05-07 11:00:00 |
| 강수량 컬럼 | `rain` |
| `rain` 결측 | 0 |
| `rain` 0 초과 행 수 | 42,257 |

`tmp`, `snow`는 컬럼은 있으나 값이 전부 비어 있어 이번 실험에서는 사용하지 않았다.

## 3. 학습 및 평가 기간

시간순 분할을 사용하였다.

| 구분 | 기간 |
|---|---|
| 학습 | 2023년 ~ 2024년 |
| 평가 | 2025년 |

## 4. 예측 목표

### 4.1 유입량 예측

```text
target_inflow_3h = inflowqy(t+3)
```

현재 시각의 강수량, 과거 강수량, 현재/과거 유입량, 댐 상태로 3시간 뒤 유입량을 예측하였다.

### 4.2 방류량 예측

```text
target_discharge_3h = totdcwtrqy(t+3)
```

현재/과거 방류량, 현재/과거 유입량, 강수량, 댐 상태를 이용해 3시간 뒤 방류량을 예측하였다.

추가로 1단계에서 예측한 3시간 뒤 유입량을 방류량 예측 입력에 넣어보았다.

## 5. 사용 모델

20개 댐 전체 데이터는 행 수가 많아 일반 `GradientBoosting`, `RandomForest`, `XGBoost` 전체 비교가 오래 걸렸다.

따라서 1차 실험에서는 대용량에 비교적 빠른 `HistGradientBoostingRegressor`를 사용하였다.

```text
HistGradientBoostingRegressor
```

## 6. 유입량 예측 결과

| 항목 | 값 |
|---|---:|
| 모델 | HistGradientBoosting |
| 학습 행 수 | 349,495 |
| 평가 행 수 | 174,595 |
| 목표 평균 | 14.881 |
| 목표 최대 | 49,802.508 |
| 기준선 MAE | 11.409 |
| 모델 MAE | 10.382 |
| 기준선 RMSE | 173.360 |
| 모델 RMSE | 125.220 |
| 기준선 R2 | -0.732 |
| 모델 R2 | 0.097 |

유입량 예측은 기준선보다 MAE와 RMSE가 개선되었다.

특히 RMSE가 크게 줄어든 것은 큰 오차 구간에서 모델이 어느 정도 도움이 된다는 뜻이다.

다만 R2는 0.097로 아직 낮다. 전체 20개 댐을 한 모델로 묶었고, 댐별 유역 특성 차이가 큰 상태라 추가 개선이 필요하다.

## 7. 방류량 예측 결과

| 입력 조합 | 모델 | 학습 행 수 | 평가 행 수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---:|---:|---:|---:|---:|---:|
| 기본피처 | HistGradientBoosting | 349,426 | 174,575 | 8.044 | 8.077 | 0.266 | 0.560 |
| 예측 유입량 추가 | HistGradientBoosting | 349,426 | 174,575 | 8.044 | 8.672 | 0.266 | 0.539 |
| 실제 미래 유입량 참고 | HistGradientBoosting | 349,426 | 174,575 | 8.044 | 8.356 | 0.266 | 0.546 |

방류량 예측에서는 기본피처 모델이 기준선보다 R2 기준으로 크게 좋아졌다.

```text
기준선 R2 = 0.266
기본피처 모델 R2 = 0.560
```

다만 MAE는 기준선과 거의 비슷하거나 약간 높았다.

## 8. 예측 유입량 추가 효과

이번 1차 전체댐 실험에서는 예측 유입량을 방류량 모델에 추가했을 때 성능이 좋아지지 않았다.

| 입력 조합 | MAE | R2 |
|---|---:|---:|
| 기본피처 | 8.077 | 0.560 |
| 예측 유입량 추가 | 8.672 | 0.539 |
| 실제 미래 유입량 참고 | 8.356 | 0.546 |

즉 현재 단계에서는 예측 유입량이 방류량 모델에 확실한 도움을 주지 못했다.

가능한 이유는 다음과 같다.

1. 유입량 예측 모델의 R2가 아직 낮다.
2. 20개 댐을 하나의 모델로 묶으면서 댐별 특성이 충분히 반영되지 않았다.
3. 방류량은 유입량뿐 아니라 운영 정책, 저수율, 방류 계획의 영향을 크게 받는다.
4. 현재 3시간 예측에서는 현재 방류량 및 과거 방류량 자체가 매우 강한 정보다.

## 9. 결론

이번 전체 20개 댐 1차 실험의 결론은 다음과 같다.

1. 팀원 통합 데이터는 전체댐 예측 실험에 사용할 수 있다.
2. 20개 댐 전체 기준 유입량 예측은 기준선보다 MAE/RMSE가 개선되었다.
3. 유입량 예측 R2는 아직 낮아 추가 개선이 필요하다.
4. 방류량 예측은 기본피처만으로 기준선보다 R2가 크게 개선되었다.
5. 예측 유입량을 방류량 모델에 추가하는 방식은 아직 성능 개선으로 이어지지 않았다.

따라서 현 단계에서 바로 최종 모델로 쓰기보다는, 다음 개선 실험이 필요하다.

## 10. 다음 단계

다음 단계는 다음 순서가 적절하다.

1. 댐별 개별 모델과 전체 통합 모델을 비교한다.
2. 20개 댐 전체에서 유입량 급증 이벤트 구간만 따로 평가한다.
3. `dam_code`를 단순 숫자가 아니라 범주형 또는 원핫 인코딩 방식으로 처리한다.
4. 3시간뿐 아니라 6시간, 12시간 예측도 비교한다.
5. 유입량 예측 성능을 먼저 끌어올린 뒤 방류량 2단계 모델에 다시 연결한다.

## 11. 생성 산출물

| 파일명 | 설명 |
|---|---|
| `전체댐_유입량_방류량_예측실험.py` | 전체 20개 댐 유입량/방류량 1차 예측 스크립트 |
| `전체댐_유입량예측_평가결과.csv` | 유입량 예측 평가 결과 |
| `전체댐_유입량예측_예측결과.csv` | 유입량 예측값 |
| `전체댐_예측유입량기반_방류량예측_평가결과.csv` | 방류량 예측 평가 결과 |
| `전체댐_유입량_방류량_예측실험_1차보고서.md` | 본 보고서 |

