# 보령댐 강우/장마철 기준 유입량 예측 분석 보고서

## 1. 분석 목적

이전 유입량 예측 실험에서는 전체 기간을 기준으로 모델을 평가하였다. 하지만 비가 오지 않는 평상시에는 유입량이 크게 변하지 않기 때문에, 단순히 현재 유입량을 그대로 유지하는 기준선이 매우 강하게 작동한다.

이번 분석의 목적은 전체 기간이 아니라 다음과 같은 구간만 따로 분리하여 유입량 예측 성능을 확인하는 것이다.

- 강우가 있었던 시점
- 강한 강우가 있었던 시점
- 장마철
- 강우일 또는 장마철

즉, 우리가 실제로 관심 있는 “비가 와서 유입량이 변할 가능성이 있는 구간”에서 모델이 의미 있는지 확인하였다.

## 2. 사용 데이터

사용한 데이터는 기존 병합 데이터이다.

```text
보령댐_운영_강수량_병합데이터.csv
```

이 데이터에는 다음 정보가 포함되어 있다.

| 구분 | 주요 컬럼 |
|---|---|
| 시간 | `obsrdt` |
| 유입량 | `inflowqy` |
| 강수량 | `rain_mm`, `rain_lag_*`, `rain_sum_*` |
| 댐 상태 | `lowlevel`, `rsvwtqy`, `rsvwtrt` |

임의값은 사용하지 않았고, 결측이 있는 행은 제거하였다.

## 3. 예측 목표

이번 분석의 목표값은 3시간 뒤 유입량이다.

```text
target_inflow_3h = inflowqy(t+3)
```

즉 현재 시점의 유입량, 과거 유입량, 강수량, 저수 상태를 이용해 3시간 뒤 유입량을 예측하였다.

## 4. 학습/평가 기간

2026년 데이터는 5월 7일까지밖에 없어 장마철 평가에 적합하지 않다. 따라서 장마철 평가가 가능하도록 다음과 같이 기간을 나누었다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | 2023년 1월 1일 ~ 2024년 12월 31일 |
| 평가 데이터 | 2025년 1월 1일 ~ 2025년 12월 31일 |

시간순 분할을 사용했으며, 무작위 분할은 사용하지 않았다.

## 5. 구간 정의

이번 분석에서는 네 가지 구간을 따로 평가하였다.

| 구간명 | 정의 |
|---|---|
| 강우일 | `rain_sum_24h > 0` |
| 강한 강우 | `rain_sum_24h >= 10mm` |
| 장마철 | 6월 ~ 9월 |
| 강우일 또는 장마철 | `rain_sum_24h > 0` 또는 6월 ~ 9월 |

여기서 `rain_sum_24h`는 최근 24시간 누적 강수량이다.

## 6. 사용 피처

입력 피처는 다음과 같이 구성하였다.

### 6.1 유입량 피처

| 피처 | 의미 |
|---|---|
| `inflowqy` | 현재 유입량 |
| `inflow_lag_1h` | 1시간 전 유입량 |
| `inflow_lag_3h` | 3시간 전 유입량 |
| `inflow_lag_6h` | 6시간 전 유입량 |
| `inflow_mean_3h` | 최근 3시간 평균 유입량 |
| `inflow_mean_6h` | 최근 6시간 평균 유입량 |
| `inflow_mean_12h` | 최근 12시간 평균 유입량 |

### 6.2 강수량 피처

| 피처 | 의미 |
|---|---|
| `rain_mm` | 현재 강수량 |
| `rain_lag_1h` | 1시간 전 강수량 |
| `rain_lag_3h` | 3시간 전 강수량 |
| `rain_lag_6h` | 6시간 전 강수량 |
| `rain_lag_12h` | 12시간 전 강수량 |
| `rain_sum_3h` | 최근 3시간 누적 강수량 |
| `rain_sum_6h` | 최근 6시간 누적 강수량 |
| `rain_sum_12h` | 최근 12시간 누적 강수량 |
| `rain_sum_24h` | 최근 24시간 누적 강수량 |
| `rain_sum_48h` | 최근 48시간 누적 강수량 |
| `rain_sum_72h` | 최근 72시간 누적 강수량 |

### 6.3 댐 상태 피처

| 피처 | 의미 |
|---|---|
| `lowlevel` | 저수위 |
| `rsvwtqy` | 저수량 |
| `rsvwtrt` | 저수율 |

## 7. 모델

모델은 XGBoost 회귀 모델을 사용하였다.

```text
XGBRegressor(objective="reg:squarederror")
```

이번 분석은 구간별 가능성 확인이 목적이므로, 하이퍼파라미터 튜닝은 수행하지 않았다.

## 8. 기준선

기준선은 다음과 같이 정의하였다.

```text
3시간 뒤 유입량 예측값 = 현재 유입량
```

이 기준선을 꼭 최종 목표로 삼아야 하는 것은 아니다. 다만 모델이 실제로 의미 있는지 확인하기 위한 비교 기준으로 사용하였다.

특히 비가 오지 않거나 유입량 변화가 적은 구간에서는 기준선이 강하게 작동한다. 따라서 이번 분석에서는 전체 기간이 아니라 강우/장마철 구간에서 기준선과 모델을 비교하였다.

## 9. 평가 결과

| 평가 구간 | 필터 후 행 수 | 학습 행 수 | 평가 행 수 | 목표 평균 | 목표 최대 | 기준선 MAE | 기준선 R2 | XGBoost MAE | XGBoost R2 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 강우일 | 7,074 | 4,324 | 2,378 | 13.282 | 753.346 | 5.196 | 0.623 | 6.576 | 0.577 |
| 강한 강우 | 4,163 | 2,597 | 1,427 | 19.454 | 753.346 | 7.644 | 0.605 | 9.983 | 0.566 |
| 장마철 | 8,739 | 5,824 | 2,915 | 10.687 | 753.346 | 4.063 | 0.627 | 6.198 | 0.396 |
| 강우일 또는 장마철 | 12,000 | 7,591 | 4,037 | 9.461 | 753.346 | 3.525 | 0.635 | 4.244 | 0.553 |

## 10. 결과 해석

강우/장마철 구간만 따로 보아도 현재 유입량 유지 기준선이 XGBoost보다 더 좋은 결과를 보였다.

특히 MAE 기준으로 보면 모든 구간에서 기준선이 더 낮다.

하지만 이전 전체 기간 분석과 비교했을 때 중요한 변화도 있다.

1. 평가 대상의 목표 유입량 평균과 최대값이 커졌다.
2. 강수량 관련 피처가 변수 중요도 상위에 들어왔다.
3. 강한 강우 구간에서는 `rain_sum_72h`, `rain_lag_6h` 같은 피처가 중요하게 나타났다.

즉 모델이 강수량 정보를 완전히 무시하는 것은 아니다. 다만 현재 데이터와 기본 XGBoost 설정만으로는 기준선을 이길 만큼 안정적인 예측을 만들지는 못했다.

## 11. 구간별 중요 변수

### 11.1 강우일

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `inflowqy` | 0.325 |
| 2 | `inflow_mean_3h` | 0.076 |
| 3 | `rain_lag_6h` | 0.076 |
| 4 | `rain_sum_72h` | 0.075 |
| 5 | `inflow_lag_3h` | 0.061 |

### 11.2 강한 강우

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `inflowqy` | 0.304 |
| 2 | `rain_sum_72h` | 0.104 |
| 3 | `rain_lag_6h` | 0.097 |
| 4 | `inflow_lag_6h` | 0.093 |
| 5 | `inflow_lag_1h` | 0.071 |

### 11.3 장마철

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `inflowqy` | 0.353 |
| 2 | `inflow_lag_1h` | 0.138 |
| 3 | `rain_lag_6h` | 0.113 |
| 4 | `rain_sum_12h` | 0.080 |
| 5 | `rain_lag_3h` | 0.065 |

### 11.4 강우일 또는 장마철

| 순위 | 변수 | 중요도 |
|---:|---|---:|
| 1 | `inflowqy` | 0.265 |
| 2 | `rain_sum_72h` | 0.099 |
| 3 | `rain_lag_12h` | 0.074 |
| 4 | `inflow_lag_3h` | 0.072 |
| 5 | `rain_lag_6h` | 0.069 |

## 12. 결론

이번 분석의 결론은 다음과 같다.

1. 강우/장마철만 따로 분리해서 유입량 예측을 수행할 수 있다.
2. 강수량 피처는 모델에서 실제로 사용되고 있으며, 중요 변수 상위에 나타난다.
3. 그러나 현재 단일 보령댐 격자 강수량과 기본 XGBoost 설정만으로는 현재 유입량 유지 기준선을 이기지 못했다.
4. 기준선이 강한 이유는 3시간 뒤 유입량이 현재 유입량과 매우 비슷한 경우가 많기 때문이다.
5. 모델을 더 의미 있게 만들려면 단순 강우일보다 “유입량 급증 직전/직후” 이벤트를 따로 잘라서 평가해야 한다.

## 13. 다음 단계

다음 단계는 단순히 비가 온 날짜가 아니라, 유입량이 실제로 급증한 이벤트를 기준으로 평가하는 것이다.

예를 들면 다음 조건을 사용할 수 있다.

- 3시간 뒤 유입량이 현재보다 일정 비율 이상 증가한 시점
- `target_inflow_3h - inflowqy`가 상위 5%인 시점
- 유입량이 상위 5% 이상인 시점
- 강수 후 6~24시간 사이 유입량이 증가한 시점

이런 조건으로 잘라야 모델이 “평상시 유지”가 아니라 “변화 감지”를 잘하는지 확인할 수 있다.

## 14. 생성된 산출물

| 파일명 | 설명 |
|---|---|
| `보령댐_강우장마철_유입량예측.py` | 강우/장마철 유입량 예측 스크립트 |
| `보령댐_강우장마철_유입량예측_평가결과.csv` | 구간별 평가 결과 |
| `보령댐_강우장마철_유입량예측_예측결과.csv` | 구간별 예측 결과 |
| `보령댐_강우장마철_유입량예측_피처중요도.csv` | 구간별 피처 중요도 |
| `보령댐_강우장마철_유입량예측_분석보고서.md` | 본 보고서 |

