# 보령댐 예측 유입량 기반 방류량 예측 보고서

## 1. 실험 목적

이번 실험은 이전 단계에서 만든 유입량 예측 결과를 방류량 예측에 연결하기 위한 것이다.

전체 구조는 다음과 같다.

```text
1단계: 현재/과거 유입량 + 강수량 + 댐 상태 → 3시간 뒤 유입량 예측
2단계: 예측된 3시간 뒤 유입량 + 현재 방류량/댐 상태/강수량 → 3시간 뒤 방류량 예측
```

즉 단순히 유입량 예측에서 끝나는 것이 아니라, 예측 유입량을 실제 방류량 예측 모델의 입력으로 사용해 보았다.

## 2. 중요한 원칙

이번 실험에서는 미래 정보를 임의로 넣지 않았다.

실제 운영 시점에서는 3시간 뒤 유입량을 알 수 없기 때문에, 방류량 예측 모델에 넣는 유입량은 실제 미래 유입량이 아니라 **유입량 모델이 예측한 값**을 사용하였다.

다만 비교를 위해 `실제 미래 유입량`을 넣은 경우도 함께 실험하였다. 이것은 실제 운영에서 사용할 수 없는 값이므로, 모델 성능의 참고용 상한선으로만 해석해야 한다.

## 3. 사용 데이터

사용한 데이터는 다음 파일이다.

```text
보령댐_운영_강수량_병합데이터.csv
```

학습과 평가는 다음과 같이 나누었다.

| 구분 | 기간 |
|---|---|
| 학습 | 2023년 ~ 2024년 |
| 평가 | 2025년 |

## 4. 1단계 유입량 예측

유입량 예측 목표값은 다음과 같다.

```text
target_inflow_3h = inflowqy(t+3)
```

유입량 예측 모델은 RandomForest를 사용하였다.

학습 데이터 내부에서는 시간순 OOF 방식으로 예측 유입량을 만들었다. 이렇게 한 이유는 방류량 모델 학습 시 실제 미래 유입량을 그대로 넣으면 데이터 누수가 생기기 때문이다.

2025년 평가 구간에서는 2023~2024년으로 학습한 유입량 모델이 3시간 뒤 유입량을 예측하였다.

유입량 예측 성능은 다음과 같다.

| 대상 | 평가 건수 | MAE | RMSE | R2 |
|---|---:|---:|---:|---:|
| 3시간 뒤 유입량 | 8,735 | 2.285 | 12.500 | 0.608 |

즉 유입량 예측 자체는 어느 정도 가능했다.

## 5. 2단계 방류량 예측

방류량 예측 목표값은 다음과 같다.

```text
target_discharge_3h = totdcwtrqy(t+3)
```

비교한 입력 조합은 다음 3가지이다.

| 입력 조합 | 설명 |
|---|---|
| 기본피처 | 현재/과거 방류량, 현재/과거 유입량, 강수량, 댐 상태 |
| 기본피처 + 예측 유입량 | 기본피처에 유입량 모델이 예측한 3시간 뒤 유입량 추가 |
| 기본피처 + 실제 미래 유입량 | 참고용. 실제 운영에서는 사용할 수 없는 상한선 |

방류량 예측 모델은 RandomForest, GradientBoosting, XGBoost를 비교하였다.

## 6. 전체 결과 요약

각 평가 구간에서 가장 좋은 모델을 기준으로 정리하면 다음과 같다.

| 평가 구간 | 입력 조합 | 최고 모델 | 평가 건수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---|---:|---:|---:|---:|---:|
| 2025 전체 | 기본피처 | RandomForest | 8,735 | 0.297 | 0.784 | 0.943 | 0.842 |
| 2025 전체 | 예측 유입량 추가 | RandomForest | 8,735 | 0.297 | 0.770 | 0.943 | 0.837 |
| 2025 전체 | 실제 미래 유입량 참고 | RandomForest | 8,735 | 0.297 | 0.684 | 0.943 | 0.866 |
| 강우일 | 기본피처 | RandomForest | 2,366 | 0.456 | 1.628 | 0.939 | 0.823 |
| 강우일 | 예측 유입량 추가 | RandomForest | 2,366 | 0.456 | 1.599 | 0.939 | 0.813 |
| 강한 강우 | 기본피처 | RandomForest | 1,415 | 0.614 | 2.387 | 0.936 | 0.817 |
| 강한 강우 | 예측 유입량 추가 | RandomForest | 1,415 | 0.614 | 2.343 | 0.936 | 0.806 |
| 유입량 증가 상위 5% | 기본피처 | XGBoost | 418 | 0.362 | 1.300 | 0.969 | 0.888 |
| 유입량 증가 상위 5% | 예측 유입량 추가 | RandomForest | 418 | 0.362 | 1.519 | 0.969 | 0.780 |
| 방류량 상위 5% | 기본피처 | RandomForest | 520 | 1.389 | 4.367 | 0.905 | 0.772 |
| 방류량 상위 5% | 예측 유입량 추가 | RandomForest | 520 | 1.389 | 4.602 | 0.905 | 0.741 |

## 7. 해석

### 7.1 유입량 예측은 어느 정도 된다

3시간 뒤 유입량 예측은 R2 0.608로 나왔다. 완벽하지는 않지만, 유입량 자체를 어느 정도 예측할 수 있다는 의미이다.

이전 이벤트 평가에서도 유입량 급증 구간에서는 머신러닝 모델이 기준선보다 좋은 결과를 보였다.

### 7.2 그러나 방류량 예측에서는 기준선이 너무 강하다

방류량 예측에서는 현재 방류량 유지 기준선이 매우 강하게 나왔다.

예를 들어 2025년 전체 구간에서:

```text
기준선 MAE = 0.297
모델 MAE = 0.770 ~ 0.784
```

즉 현재 방류량을 그대로 3시간 뒤 방류량으로 예측해도 오차가 매우 작다.

이는 보령댐의 방류량이 3시간 단위에서는 급격히 변하지 않는 시간이 많다는 뜻이다.

### 7.3 예측 유입량 추가 효과는 작다

예측 유입량을 추가하면 일부 구간에서 MAE가 아주 조금 개선되었다.

예를 들어:

```text
2025 전체: 0.784 → 0.770
강우일: 1.628 → 1.599
강한 강우: 2.387 → 2.343
```

하지만 개선 폭은 크지 않았다.

그리고 방류량 상위 5%나 유입량 증가 상위 5% 구간에서는 오히려 예측 유입량을 넣은 모델이 더 나빠진 경우도 있었다.

### 7.4 실제 미래 유입량을 넣으면 성능이 조금 좋아진다

참고용으로 실제 3시간 뒤 유입량을 넣었을 때는 일부 구간에서 성능이 좋아졌다.

예를 들어 2025 전체:

```text
기본피처 MAE = 0.784
예측 유입량 추가 MAE = 0.770
실제 미래 유입량 참고 MAE = 0.684
```

이것은 미래 유입량 정보가 방류량 예측에 도움이 될 수 있다는 뜻이다.

하지만 실제 운영에서는 미래 유입량을 알 수 없으므로, 유입량 예측 모델의 정확도를 더 높여야 이 효과를 제대로 얻을 수 있다.

## 8. 결론

이번 실험의 결론은 다음과 같다.

1. 우리가 만든 유입량 예측 모델은 실제로 사용되었다.
2. 3시간 뒤 유입량 예측은 어느 정도 가능했다.
3. 예측 유입량을 방류량 예측 입력으로 넣는 2단계 구조도 구현 가능하다.
4. 하지만 현재 보령댐 2025년 방류량은 현재 방류량 유지 기준선이 매우 강하다.
5. 예측 유입량을 추가해도 방류량 예측 성능 개선은 아직 작다.
6. 실제 미래 유입량을 넣으면 성능이 좋아지는 경향이 있어, 유입량 예측 정확도를 높이면 방류량 예측에도 도움이 될 가능성이 있다.

따라서 현재 단계에서 “유입량 예측 → 방류량 예측” 구조는 가능하지만, 보령댐 단일 데이터에서는 아직 최종 모델로 충분하다고 보기는 어렵다.

## 9. 다음 단계

다음 단계는 다음 순서가 좋다.

1. 방류량이 실제로 급증한 이벤트만 따로 평가한다.
2. 예측 시간을 3시간이 아니라 6시간, 12시간으로 늘려본다.
3. 유입량 예측 모델의 정확도를 먼저 개선한다.
4. 보령댐 외 20개 다목적댐으로 확장하여 이벤트 수를 늘린다.
5. 단일 댐 모델보다 전체 댐 통합 모델이 더 안정적인지 비교한다.

현재 보령댐 하나만으로는 이벤트 수가 제한적이다. 20개 댐 전체를 사용하면 유입량 급증과 방류량 변화 사례가 많아져 모델이 더 잘 배울 가능성이 높다.

## 10. 생성 산출물

| 파일명 | 설명 |
|---|---|
| `보령댐_예측유입량기반_방류량예측.py` | 2단계 유입량 기반 방류량 예측 스크립트 |
| `보령댐_예측유입량기반_유입량예측성능.csv` | 1단계 유입량 예측 성능 |
| `보령댐_예측유입량기반_방류량예측_평가결과.csv` | 2단계 방류량 예측 평가 결과 |
| `보령댐_예측유입량기반_방류량예측_이벤트예측결과.csv` | 이벤트 구간 상세 예측 결과 |
| `보령댐_예측유입량기반_방류량예측_보고서.md` | 본 보고서 |

