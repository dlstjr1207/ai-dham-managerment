# 보령댐 유입량 예측 여러 모델 비교 및 이벤트 구간 평가 보고서

## 1. 분석 목적

이번 분석은 이전 실험에서 나온 의문을 확인하기 위해 수행하였다.

이전까지의 결과는 다음과 같았다.

- XGBoost는 후보 모델로 적절해 보인다.
- 하지만 전체 기간이나 단순 강우일 기준에서는 현재 유입량 유지 기준선보다 항상 좋지는 않았다.
- 비가 안 오거나 유입량 변화가 작은 구간에서는 기준선이 너무 강하다.

그래서 이번에는 두 가지를 확인하였다.

1. XGBoost 말고 다른 모델을 써도 예측이 잘 안 되는지 확인
2. 전체 기간이 아니라 유입량이 실제로 급증한 이벤트 구간에서 모델이 의미가 있는지 확인

이번 분석도 목표는 **3시간 뒤 유입량 예측**이다.

```text
현재/과거 유입량 + 강수량 + 댐 상태 → 3시간 뒤 유입량
```

따라서 우리가 만든 유입량 예측 구조를 계속 사용하고 있다.

## 2. 사용 데이터

사용한 데이터는 기존 병합 데이터이다.

```text
보령댐_운영_강수량_병합데이터.csv
```

임의값은 사용하지 않았다. 결측이 있는 행은 제거하였다.

## 3. 예측 목표

목표값은 3시간 뒤 유입량이다.

```text
target_inflow_3h = inflowqy(t+3)
```

현재 시각의 유입량 `inflowqy(t)`와 과거 유입량, 강수량, 저수 상태를 이용해 3시간 뒤 유입량을 예측한다.

## 4. 학습/평가 기간

시간순 분할을 사용하였다.

| 구분 | 기간 |
|---|---|
| 학습 | 2023년 ~ 2024년 |
| 평가 | 2025년 |

2026년은 5월 초까지밖에 없어 장마철 및 연중 이벤트 평가에 적합하지 않으므로, 이번 분석에서는 2025년을 평가 연도로 사용하였다.

## 5. 사용 피처

사용한 주요 피처는 다음과 같다.

### 5.1 유입량 피처

- `inflowqy`
- `inflow_lag_1h`
- `inflow_lag_3h`
- `inflow_lag_6h`
- `inflow_lag_12h`
- `inflow_mean_3h`
- `inflow_mean_6h`
- `inflow_mean_12h`
- `inflow_mean_24h`

### 5.2 강수량 피처

- `rain_mm`
- `rain_lag_1h`
- `rain_lag_3h`
- `rain_lag_6h`
- `rain_lag_12h`
- `rain_sum_3h`
- `rain_sum_6h`
- `rain_sum_12h`
- `rain_sum_24h`
- `rain_sum_48h`
- `rain_sum_72h`

### 5.3 댐 상태 피처

- `lowlevel`
- `rsvwtqy`
- `rsvwtrt`

## 6. 비교한 모델

이번에는 XGBoost만 사용하지 않고 여러 회귀 모델을 비교하였다.

| 모델 | 설명 |
|---|---|
| Ridge | 선형 회귀 기반 모델 |
| RandomForest | 여러 결정트리 평균 기반 모델 |
| ExtraTrees | 무작위성이 더 강한 트리 앙상블 |
| GradientBoosting | 순차적으로 오차를 줄이는 부스팅 모델 |
| XGBoost | 고성능 그래디언트 부스팅 모델 |
| MLP | 신경망 회귀 모델 |

## 7. 평가 구간

평가 구간은 다음과 같이 나누었다.

| 평가 구간 | 정의 |
|---|---|
| 2025 전체 | 2025년 전체 평가 데이터 |
| 2025 강우일 | 최근 24시간 누적 강수량이 0보다 큰 시점 |
| 2025 강한강우 | 최근 24시간 누적 강수량이 10mm 이상인 시점 |
| 2025 장마철 | 6월 ~ 9월 |
| 2025 유입량 증가 상위 5% | 3시간 뒤 유입량 증가량이 학습 기간 기준 상위 5% 이상 |
| 2025 유입량 상위 5% | 3시간 뒤 유입량 자체가 학습 기간 기준 상위 5% 이상 |
| 2025 강한강우 + 유입량 증가 상위 5% | 강한 강우와 유입량 급증이 함께 나타난 시점 |

이번 분석에서 중요한 구간은 `유입량 증가 상위 5%`와 `강한강우 + 유입량 증가 상위 5%`이다. 이 구간은 단순히 비가 온 날이 아니라, 실제로 유입량이 크게 변한 시점이다.

## 8. 기준선

기준선은 다음과 같다.

```text
3시간 뒤 유입량 예측값 = 현재 유입량
```

이 기준선은 모델이 반드시 이겨야 하는 절대 기준이라기보다, 예측이 실제로 의미 있는지 보기 위한 비교 기준이다.

## 9. 핵심 결과

### 9.1 전체/강우/장마철 구간

전체 기간이나 일반 강우일, 장마철 구간에서는 여전히 기준선이 강했다.

| 학습 방식 | 평가 구간 | 최고 모델 | 평가 건수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---|---:|---:|---:|---:|---:|
| 전체 학습 | 2025 전체 | GradientBoosting | 7,778 | 2.310 | 2.300 | 0.618 | 0.627 |
| 전체 학습 | 2025 강우일 | GradientBoosting | 2,190 | 5.304 | 5.593 | 0.593 | 0.608 |
| 전체 학습 | 2025 강한강우 | GradientBoosting | 1,334 | 7.729 | 8.399 | 0.575 | 0.589 |
| 전체 학습 | 2025 장마철 | GradientBoosting | 2,655 | 4.188 | 4.433 | 0.597 | 0.615 |

해석하면 다음과 같다.

- 전체 구간에서는 GradientBoosting이 기준선과 거의 비슷하거나 조금 나았다.
- 강우일/강한강우/장마철에서는 R2는 약간 나아지는 경우가 있지만, MAE는 기준선보다 나쁜 경우가 많았다.
- 즉 단순히 “비가 온 날”로 자르는 것만으로는 충분하지 않다.

### 9.2 유입량 급증 이벤트 구간

유입량이 실제로 급증한 이벤트 구간에서는 결과가 달라졌다.

| 학습 방식 | 평가 구간 | 최고 모델 | 평가 건수 | 기준선 MAE | 모델 MAE | 기준선 R2 | 모델 R2 |
|---|---|---|---:|---:|---:|---:|---:|
| 전체 학습 | 유입량 증가 상위 5% | RandomForest | 371 | 16.749 | 13.546 | 0.546 | 0.702 |
| 전체 학습 | 강한강우 + 유입량 증가 상위 5% | RandomForest | 202 | 27.712 | 22.715 | 0.503 | 0.674 |
| 강우/장마철 학습 | 유입량 증가 상위 5% | RandomForest | 371 | 16.749 | 13.347 | 0.546 | 0.704 |
| 강우/장마철 학습 | 강한강우 + 유입량 증가 상위 5% | RandomForest | 202 | 27.712 | 22.411 | 0.503 | 0.675 |

이 결과가 가장 중요하다.

유입량이 실제로 급증하는 구간에서는 RandomForest가 현재 유입량 유지 기준선을 이겼다.

특히 `강한강우 + 유입량 증가 상위 5%` 구간에서는:

```text
기준선 MAE = 27.712
RandomForest MAE = 22.411
```

즉 오차가 약 5.3 정도 줄었다.

## 10. XGBoost가 적절한가?

XGBoost는 여전히 적절한 후보 모델이다. 다만 이번 결과만 보면 보령댐 유입량 이벤트 예측에서는 XGBoost가 항상 최고는 아니었다.

이번 실험에서는 다음 경향이 나타났다.

- 전체/강우/장마철 구간: GradientBoosting이 가장 안정적
- 유입량 급증 이벤트 구간: RandomForest가 가장 좋음
- 선형 모델 Ridge는 유입량 상위 구간에서 일부 경쟁력 있음
- MLP 신경망은 현재 구성에서는 안정적이지 않음
- XGBoost는 후보로 유지하되, 단독 최종 모델로 확정하기는 이르다

따라서 현재 결론은 다음과 같다.

```text
XGBoost는 적절한 후보지만, 보령댐 유입량 급증 이벤트에서는 RandomForest/GradientBoosting도 반드시 함께 비교해야 한다.
```

## 11. “예측이 안 된다”는 뜻인가?

아니다.

전체 기간이나 단순 강우일에서는 기준선이 강해서 모델이 잘 안 되는 것처럼 보였다. 하지만 유입량이 실제로 급증한 이벤트 구간만 보면 모델이 기준선을 이기는 결과가 나왔다.

따라서 더 정확한 결론은 다음과 같다.

```text
평상시 전체 예측은 기준선이 강하다.
하지만 유입량 급증 이벤트 예측에서는 머신러닝 모델이 의미가 있다.
```

이건 우리 프로젝트 방향에 중요한 의미가 있다. 우리가 정말 원하는 것은 “평상시 유입량 유지”가 아니라, 강우 이후 유입량이 튀는 시점을 미리 잡는 것이다.

## 12. 우리가 한 유입량 예측을 쓰고 있는가?

그렇다.

이번 분석은 모두 유입량 예측이다.

사용한 목표값은 다음과 같다.

```text
target_inflow_3h = 3시간 뒤 유입량
```

그리고 입력값은 현재/과거 유입량, 강수량, 댐 상태이다.

즉 방류량 예측 이전 단계로, 유입량 예측 모델을 따로 검증하고 있는 것이다.

## 13. 다음 단계

다음 단계는 아래 순서가 좋다.

1. 유입량 급증 이벤트 기준 모델을 RandomForest와 GradientBoosting 중심으로 정리한다.
2. 같은 이벤트 평가를 20개 다목적댐 전체에 확장한다.
3. 보령댐은 단일 격자 강수량만 쓰고 있으므로, 주변 격자 강수량을 추가할 수 있는지 확인한다.
4. 유입량 예측 결과를 방류량 예측 모델의 입력으로 연결한다.

최종적으로는 다음 구조가 자연스럽다.

```text
강수량 + 과거 유입량 + 댐 상태
→ 3시간/6시간 뒤 유입량 예측
→ 예측 유입량 + 댐 상태
→ 방류량 예측
```

## 14. 생성 산출물

| 파일명 | 설명 |
|---|---|
| `보령댐_유입량_모델비교_이벤트평가.py` | 여러 모델 비교 및 이벤트 평가 스크립트 |
| `보령댐_유입량_여러모델_이벤트평가결과.csv` | 전체 평가 결과 |
| `보령댐_유입량_여러모델_이벤트예측결과.csv` | 이벤트 구간 예측 상세 결과 |
| `보령댐_유입량_여러모델_이벤트평가_보고서.md` | 본 보고서 |

