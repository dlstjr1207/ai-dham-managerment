# 보령댐 XGBoost 1시간/3시간/6시간 뒤 방류량 예측 실험 보고서

## 1. 실험 목적

이번 실험의 목적은 보령댐 단일 데이터를 대상으로 XGBoost 모델이 현재 시각의 운영 데이터와 강수량 데이터를 이용해 미래 총방류량을 예측할 수 있는지 확인하는 것이다.

이전 단계에서는 1시간 뒤 예측만 수행하였다. 이번 단계에서는 같은 전처리 원칙을 유지하면서 예측 목표 시점을 다음과 같이 확장하였다.

- 1시간 뒤 총방류량
- 3시간 뒤 총방류량
- 6시간 뒤 총방류량

이번 실험에서도 임의값, 가짜 데이터, 임의 보정값은 사용하지 않았다. 결측치는 값으로 채우지 않고 해당 행을 제거하였다.

## 2. 사용 데이터

사용한 입력 데이터는 다음 파일이다.

```text
보령댐_운영_강수량_병합데이터.csv
```

데이터 개요는 다음과 같다.

| 항목 | 내용 |
|---|---|
| 대상 댐 | 보령댐 |
| 전체 행 수 | 29,339건 |
| 시간 범위 | 2023-01-01 01:00:00 ~ 2026-05-07 11:00:00 |
| 시간 중복 | 0건 |
| 강수량 매칭 누락 | 0건 |

## 3. 목표값 생성 방식

원본 총방류량 컬럼은 다음과 같다.

```text
totdcwtrqy
```

이번 실험에서는 현재 시각 `t`의 입력 변수로 미래 시각의 총방류량을 예측하도록 목표값을 만들었다.

| 목표값 컬럼 | 의미 |
|---|---|
| `target_totdcwtrqy_1h` | 현재 시각 기준 1시간 뒤 총방류량 |
| `target_totdcwtrqy_3h` | 현재 시각 기준 3시간 뒤 총방류량 |
| `target_totdcwtrqy_6h` | 현재 시각 기준 6시간 뒤 총방류량 |

생성 방식은 다음과 같다.

```text
target_totdcwtrqy_1h = totdcwtrqy를 1칸 미래로 이동
target_totdcwtrqy_3h = totdcwtrqy를 3칸 미래로 이동
target_totdcwtrqy_6h = totdcwtrqy를 6칸 미래로 이동
```

여기서 한 칸은 1시간이다.

## 4. 전처리 방식

전처리 방식은 이전 보령댐 전처리 실험과 동일하게 유지하였다.

1. `obsrdt` 기준으로 시간순 정렬
2. 미래 목표값 생성
3. 목표값 또는 입력 변수에 결측이 있는 행 제거
4. 시간 기준으로 학습/테스트 분리
5. XGBoost 모델 학습 및 평가

중요한 점은 결측값을 임의로 채우지 않았다는 것이다.

사용하지 않은 방식은 다음과 같다.

- 평균값 대체
- 중앙값 대체
- 0 대체
- 임의 보간
- 임의 강수량 생성
- 임의 방류량 생성

## 5. 학습/테스트 분리

시계열 데이터이므로 랜덤 분할은 사용하지 않았다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | 2023-01-01 ~ 2025-12-31 |
| 테스트 데이터 | 2026-01-01 ~ 2026-05-07 |

미래 데이터가 학습에 섞이는 것을 막기 위해 시간순 분할을 적용하였다.

예측 시간이 길어질수록 마지막 몇 시간은 미래 목표값을 만들 수 없으므로 테스트 종료 시각이 조금씩 앞당겨진다.

| 예측 시간 | 테스트 종료 시각 |
|---:|---|
| 1시간 뒤 | 2026-05-07 10:00:00 |
| 3시간 뒤 | 2026-05-07 08:00:00 |
| 6시간 뒤 | 2026-05-07 05:00:00 |

## 6. 피처 조합

이번 실험에서는 5가지 피처 조합을 비교하였다.

### 6.1 운영 데이터만

```text
inflowqy
lowlevel
rf
rsvwtqy
rsvwtrt
```

### 6.2 강수량만

```text
rain_mm
rain_lag_1h
rain_lag_2h
rain_lag_3h
rain_lag_6h
rain_lag_12h
rain_lag_24h
rain_sum_3h
rain_sum_6h
rain_sum_12h
rain_sum_24h
rain_sum_48h
rain_sum_72h
```

### 6.3 운영 데이터 + 강수량

운영 데이터 피처와 강수량 피처를 함께 사용하였다.

### 6.4 운영 데이터 + 현재 방류량

현재 시각의 총방류량 `totdcwtrqy`를 입력 변수로 추가하였다.

이는 임의값이 아니라 현재 시각에 실제로 관측된 방류량이다. 1시간, 3시간, 6시간 뒤 방류량을 예측할 때 현재 방류량은 실제 운영 상황에서 사용 가능한 정보이다.

### 6.5 운영 데이터 + 강수량 + 현재 방류량

가장 많은 정보를 사용하는 조합이다.

## 7. 평가 지표

평가 지표는 다음 3가지를 사용하였다.

| 지표 | 의미 |
|---|---|
| MAE | 평균 절대 오차 |
| RMSE | 평균 제곱근 오차 |
| R2 | 결정계수 |

또한 단순 기준선으로 `현재 방류량 유지 기준선`을 함께 계산하였다.

```text
예측값 = 현재 시각의 totdcwtrqy
```

이 기준선은 시계열 예측에서 반드시 비교해야 하는 기본 기준이다. 특히 방류량이 짧은 시간 동안 크게 변하지 않는 구간에서는 매우 강한 기준선이 될 수 있다.

## 8. 전체 평가 결과

| 예측 시간 | 피처 조합 | 학습 건수 | 테스트 건수 | MAE | RMSE | R2 | 현재 방류량 기준선 MAE | 현재 방류량 기준선 R2 |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1시간 | 운영 데이터만 | 26,297 | 3,035 | 0.447 | 0.761 | -1.379 | 0.117 | 0.806 |
| 1시간 | 강수량만 | 26,296 | 3,035 | 0.616 | 1.256 | -5.479 | 0.117 | 0.806 |
| 1시간 | 운영 데이터 + 강수량 | 26,290 | 3,035 | 0.362 | 1.067 | -3.676 | 0.117 | 0.806 |
| 1시간 | 운영 데이터 + 현재 방류량 | 26,297 | 3,035 | 0.162 | 0.386 | 0.387 | 0.117 | 0.806 |
| 1시간 | 운영 데이터 + 강수량 + 현재 방류량 | 26,290 | 3,035 | 0.154 | 0.343 | 0.515 | 0.117 | 0.806 |
| 3시간 | 운영 데이터만 | 26,297 | 3,033 | 0.458 | 0.782 | -1.511 | 0.242 | 0.494 |
| 3시간 | 강수량만 | 26,296 | 3,033 | 0.617 | 1.321 | -6.168 | 0.242 | 0.494 |
| 3시간 | 운영 데이터 + 강수량 | 26,290 | 3,033 | 0.346 | 0.792 | -1.576 | 0.242 | 0.494 |
| 3시간 | 운영 데이터 + 현재 방류량 | 26,297 | 3,033 | 0.255 | 0.505 | -0.046 | 0.242 | 0.494 |
| 3시간 | 운영 데이터 + 강수량 + 현재 방류량 | 26,290 | 3,033 | 0.255 | 0.465 | 0.112 | 0.242 | 0.494 |
| 6시간 | 운영 데이터만 | 26,297 | 3,030 | 0.435 | 0.768 | -1.423 | 0.269 | 0.383 |
| 6시간 | 강수량만 | 26,296 | 3,030 | 0.592 | 1.292 | -5.854 | 0.269 | 0.383 |
| 6시간 | 운영 데이터 + 강수량 | 26,290 | 3,030 | 0.337 | 0.609 | -0.522 | 0.269 | 0.383 |
| 6시간 | 운영 데이터 + 현재 방류량 | 26,297 | 3,030 | 0.257 | 0.485 | 0.035 | 0.269 | 0.383 |
| 6시간 | 운영 데이터 + 강수량 + 현재 방류량 | 26,290 | 3,030 | 0.259 | 0.482 | 0.045 | 0.269 | 0.383 |

## 9. 예측 시간별 해석

### 9.1 1시간 뒤 예측

1시간 뒤 예측에서는 `운영 데이터 + 강수량 + 현재 방류량` 조합이 XGBoost 모델 중 가장 좋았다.

| 항목 | 값 |
|---|---:|
| MAE | 0.154 |
| RMSE | 0.343 |
| R2 | 0.515 |

하지만 현재 방류량 유지 기준선은 MAE 0.117, R2 0.806으로 더 좋았다.

즉, 1시간 뒤 예측에서는 현재 방류량을 그대로 유지하는 단순 기준선이 매우 강했다. 이는 테스트 구간에서 방류량 변화가 크지 않았기 때문으로 해석된다.

### 9.2 3시간 뒤 예측

3시간 뒤 예측에서도 `운영 데이터 + 강수량 + 현재 방류량` 조합이 XGBoost 모델 중 가장 좋았다.

| 항목 | 값 |
|---|---:|
| MAE | 0.255 |
| RMSE | 0.465 |
| R2 | 0.112 |

현재 방류량 유지 기준선은 MAE 0.242, R2 0.494였다.

3시간 뒤부터는 기준선의 성능이 1시간 예측보다 낮아졌다. 하지만 여전히 기준선이 XGBoost보다 MAE와 R2 모두에서 더 좋다.

### 9.3 6시간 뒤 예측

6시간 뒤 예측에서는 MAE 기준으로 `운영 데이터 + 현재 방류량` 조합이 가장 좋았다.

| 항목 | 값 |
|---|---:|
| MAE | 0.257 |
| RMSE | 0.485 |
| R2 | 0.035 |

`운영 데이터 + 강수량 + 현재 방류량` 조합은 MAE 0.259, RMSE 0.482, R2 0.045였다.

두 모델의 차이는 매우 작다. RMSE와 R2 기준으로는 강수량을 함께 넣은 모델이 조금 더 낫고, MAE 기준으로는 강수량을 제외한 모델이 아주 근소하게 좋다.

현재 방류량 유지 기준선은 MAE 0.269, R2 0.383이었다.

여기서 중요한 점은 6시간 뒤 예측에서는 XGBoost의 MAE가 기준선보다 약간 좋아졌다는 것이다. 다만 R2는 기준선보다 낮기 때문에, 전반적 설명력에서는 아직 부족하다.

## 10. 강수량 피처의 효과

강수량만 사용한 모델은 모든 예측 시간에서 성능이 좋지 않았다.

하지만 운영 데이터와 함께 사용할 때는 일부 개선이 있었다.

| 예측 시간 | 운영 데이터만 MAE | 운영 데이터 + 강수량 MAE | 변화 |
|---:|---:|---:|---:|
| 1시간 | 0.447 | 0.362 | 개선 |
| 3시간 | 0.458 | 0.346 | 개선 |
| 6시간 | 0.435 | 0.337 | 개선 |

즉, 강수량은 단독으로 방류량을 설명하기에는 부족하지만, 운영 데이터와 결합했을 때는 예측 오차를 줄이는 데 도움이 되었다.

현재 방류량을 포함한 경우에는 강수량 효과가 예측 시간에 따라 다르게 나타났다.

| 예측 시간 | 운영 + 현재 방류량 MAE | 운영 + 강수량 + 현재 방류량 MAE | 해석 |
|---:|---:|---:|---|
| 1시간 | 0.162 | 0.154 | 강수량 추가로 개선 |
| 3시간 | 0.255 | 0.255 | 거의 동일 |
| 6시간 | 0.257 | 0.259 | MAE 기준으로는 거의 동일하거나 약간 악화 |

따라서 현재 단계에서는 강수량이 항상 성능을 크게 올린다고 말하기는 어렵다. 다만 운영 데이터만 사용하는 모델에서는 강수량 추가 효과가 분명히 보인다.

## 11. 주요 변수 중요도

아래 표는 `운영 데이터 + 강수량 + 현재 방류량` 조합에서 예측 시간별 상위 변수 중요도를 정리한 것이다.

### 11.1 1시간 뒤 예측

| 변수 | 중요도 |
|---|---:|
| `rsvwtrt` | 0.503 |
| `totdcwtrqy` | 0.303 |
| `rain_sum_6h` | 0.059 |
| `rain_lag_12h` | 0.055 |
| `rain_lag_24h` | 0.026 |
| `rain_sum_12h` | 0.016 |
| `lowlevel` | 0.015 |
| `rsvwtqy` | 0.007 |

1시간 뒤 예측에서는 저수율과 현재 방류량의 영향이 크게 나타났다. 강수량 변수 중에서는 최근 6시간 누적 강수량과 12시간 전 강수량이 상위에 들어왔다.

### 11.2 3시간 뒤 예측

| 변수 | 중요도 |
|---|---:|
| `totdcwtrqy` | 0.452 |
| `rain_sum_3h` | 0.198 |
| `lowlevel` | 0.167 |
| `rain_lag_12h` | 0.046 |
| `rain_sum_24h` | 0.040 |
| `rain_sum_6h` | 0.034 |
| `rsvwtqy` | 0.012 |
| `rf` | 0.010 |

3시간 뒤 예측에서는 현재 방류량이 가장 중요했고, 강수량 변수 중 최근 3시간 누적 강수량이 크게 나타났다.

이는 예측 시간이 3시간으로 길어지면서 단기 누적 강수량이 모델 판단에 더 많이 반영된 것으로 볼 수 있다.

### 11.3 6시간 뒤 예측

| 변수 | 중요도 |
|---|---:|
| `totdcwtrqy` | 0.424 |
| `rsvwtrt` | 0.224 |
| `lowlevel` | 0.123 |
| `rain_sum_72h` | 0.072 |
| `rain_mm` | 0.066 |
| `inflowqy` | 0.016 |
| `rain_sum_24h` | 0.015 |
| `rain_sum_12h` | 0.010 |

6시간 뒤 예측에서는 현재 방류량, 저수율, 저수위가 중요하게 나타났다. 강수량 변수 중에서는 72시간 누적 강수량과 현재 강수량이 상위에 들어왔다.

예측 시간이 길어질수록 누적 강수량의 의미가 조금 더 커질 가능성이 있다.

## 12. 결론

이번 1시간/3시간/6시간 예측 실험의 결론은 다음과 같다.

1. 보령댐 데이터에서는 강수량을 운영 데이터와 결합했을 때 XGBoost의 MAE가 개선된다.
2. 강수량만으로는 방류량을 예측하기 어렵다.
3. 현재 방류량은 매우 강력한 입력 변수이다.
4. 1시간, 3시간 예측에서는 현재 방류량 유지 기준선이 XGBoost보다 좋다.
5. 6시간 예측에서는 XGBoost가 MAE 기준으로 기준선을 아주 조금 앞선다.
6. 그러나 R2 기준으로는 여전히 현재 방류량 유지 기준선이 더 좋다.

따라서 현재 단계에서 XGBoost 모델이 완전히 우수하다고 결론내리기는 어렵다. 하지만 예측 시간이 길어질수록 단순 기준선의 힘이 약해지고, 강수량 및 운영 변수의 의미가 조금씩 커지는 흐름은 확인되었다.

## 13. 다음 단계 제안

다음 실험은 다음 순서가 적절하다.

1. 보령댐에서 큰 방류 이벤트 구간만 따로 평가한다.
2. 12시간 뒤, 24시간 뒤 예측까지 확장한다.
3. 단순 기준선과 XGBoost를 항상 같이 비교한다.
4. 하이퍼파라미터 튜닝은 기준선 비교 이후 진행한다.
5. 보령댐에서 실험 방향이 정리되면 20개 다목적댐 전체에 같은 절차를 적용한다.

특히 현재 테스트 구간은 방류량 변동이 크지 않다. 모델의 실제 가치는 큰 강우와 방류량 변화가 발생하는 구간에서 더 분명하게 확인해야 한다.

## 14. 생성된 산출물

이번 실험으로 생성된 파일은 다음과 같다.

| 파일명 | 설명 |
|---|---|
| `보령댐_XGBoost_학습.py` | 1시간/3시간/6시간 예측 실험 스크립트 |
| `보령댐_XGBoost_1시간3시간6시간_평가결과.csv` | 전체 평가 결과 |
| `보령댐_XGBoost_1시간3시간6시간_예측결과.csv` | 테스트 구간 예측값 |
| `보령댐_XGBoost_1시간3시간6시간_피처중요도.csv` | 변수 중요도 |
| `보령댐_XGBoost_1시간3시간6시간_예측실험보고서.md` | 본 보고서 |

