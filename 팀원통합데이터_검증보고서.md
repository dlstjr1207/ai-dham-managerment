# 팀원 통합 데이터 검증 보고서

## 1. 받은 파일

팀원이 전달한 파일은 다음 3개이다.

| 파일명 | 역할 |
|---|---|
| `dam_project_dam_code_2026-05-12_220352.sql` | `dam_code` 테이블 구조 정의 |
| `dam_project_dam_operation_data_2026-05-12_220359.sql` | `dam_operation_data` 테이블 구조 정의 |
| `final_data.csv` | 실제 운영 데이터와 강수량이 합쳐진 CSV |

SQL 두 파일에는 `CREATE TABLE` 구조는 있지만 `COPY`나 `INSERT` 데이터는 들어있지 않았다. 실제 데이터는 `final_data.csv`에 들어있다.

## 2. final_data.csv 구조

CSV 컬럼은 다음과 같다.

```text
dam_code,inflowqy,lowlevel,obsrdt,rf,rsvwtqy,rsvwtrt,totdcwtrqy,tmp,rain,snow
```

주요 의미는 다음과 같다.

| 컬럼명 | 의미 |
|---|---|
| `dam_code` | 댐 코드 |
| `inflowqy` | 유입량 |
| `lowlevel` | 저수위 |
| `obsrdt` | 관측 시각 |
| `rf` | 기존 운영 데이터의 강우량 |
| `rsvwtqy` | 저수량 |
| `rsvwtrt` | 저수율 |
| `totdcwtrqy` | 총방류량 |
| `tmp` | 평균 기온 |
| `rain` | 1시간 강수량 |
| `snow` | 적설량 |

## 3. 전체 CSV 검증 결과

| 항목 | 결과 |
|---|---:|
| 전체 행 수 | 1,730,993 |
| 전체 댐코드 수 | 59 |
| 시간 범위 | 2023-01-01 01:00:00 ~ 2026-05-07 11:00:00 |
| `(dam_code, obsrdt)` 중복 | 0 |
| `rain` 결측 | 0 |
| `rain` 0 초과 행 수 | 42,257 |
| `tmp` 입력값 | 전부 비어 있음 |
| `snow` 입력값 | 전부 비어 있음 |

## 4. 20개 다목적댐 필터링 결과

기존 프로젝트에서 사용하기로 한 20개 다목적댐만 필터링하였다.

| 항목 | 결과 |
|---|---:|
| 필터링 후 행 수 | 586,780 |
| 포함 댐 수 | 20 |
| 각 댐별 기본 행 수 | 29,339 |
| 시간 범위 | 2023-01-01 01:00:00 ~ 2026-05-07 11:00:00 |
| 20개 외 댐 데이터 | 0 |
| `rain` 결측 | 0 |
| `rain` 0 초과 행 수 | 42,257 |
| `tmp` 입력값 | 전부 비어 있음 |
| `snow` 입력값 | 전부 비어 있음 |
| `inflowqy` 결측 | 564 |
| `totdcwtrqy` 결측 | 238 |

## 5. DB 연동 결과

기존 `public.dam_operation_data` 테이블은 건드리지 않고, 새 테이블을 생성하여 적재하였다.

```text
public.dam_operation_data_weather
```

적재 결과는 다음과 같다.

| 항목 | 결과 |
|---|---:|
| 적재 행 수 | 586,780 |
| 포함 댐 수 | 20 |
| 시간 범위 | 2023-01-01 01:00:00 ~ 2026-05-07 11:00:00 |
| 20개 외 댐 데이터 | 0 |
| `rain` 값 존재 | 586,780 |
| `rain` 0 초과 | 42,257 |
| `tmp` 값 존재 | 0 |
| `snow` 값 존재 | 0 |

## 6. 사용 가능 여부

이 데이터는 사용할 수 있다.

특히 이전에는 보령댐 강수량 CSV를 따로 병합했지만, 이제는 20개 다목적댐 전체에 대해 `rain` 컬럼이 함께 들어간 테이블을 사용할 수 있다.

다만 주의할 점은 다음과 같다.

1. `final_data.csv`는 59개 댐 전체 데이터이므로, 프로젝트 기준에 맞춰 20개 다목적댐만 필터링해야 한다.
2. `tmp`와 `snow`는 컬럼만 있고 실제 값은 전부 비어 있다.
3. `rain`은 전부 채워져 있으므로 강수량 피처로 사용할 수 있다.
4. 일부 댐에는 `inflowqy`, `totdcwtrqy` 결측이 있으므로 학습 전 결측 행 제거가 필요하다.
5. 기존 `dam_operation_data`는 유지하고, 강수량 포함 실험은 `dam_operation_data_weather`를 사용하는 것이 안전하다.

## 7. 결론

팀원이 준 데이터는 예측 실험에 사용하는 것이 좋다.

이유는 다음과 같다.

- 20개 다목적댐 전체에 대해 동일한 구조로 강수량이 포함되어 있음
- 시간 범위가 기존 운영 데이터와 동일함
- 중복 키가 없음
- DB에 안전하게 별도 테이블로 적재 완료됨

따라서 다음 예측 실험부터는 `public.dam_operation_data_weather`를 기준 데이터로 사용하는 것이 적절하다.

