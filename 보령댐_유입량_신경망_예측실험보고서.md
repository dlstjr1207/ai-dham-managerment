# 보령댐 유입량 신경망 예측 실험 보고서

## 1. 실험 목적

본 실험은 사용자가 제공한 논문 `신경망을 이용한 댐 유입량 예측`의 방식을 참고하여, 현재 보유한 보령댐 데이터와 강수량 CSV만으로 유입량 예측이 가능한지 확인하기 위해 수행하였다.

이번 실험에서는 방류량이 아니라 유입량 `inflowqy`를 예측 대상으로 두었다.

핵심 질문은 다음과 같다.

- 보령댐 운영 데이터와 강수량 데이터로 유입량 예측 모델을 만들 수 있는가?
- 논문처럼 3시간 평균 유입량과 3시간 평균 강수량을 사용하면 예측이 되는가?
- 신경망 모델이 단순 기준선보다 좋은가?
- 강수량 CSV는 어떤 구조이며, 모델 입력으로 어떻게 변환되는가?

## 2. 논문 방식 요약

논문에서는 댐 유입량 예측을 위해 다음 방식을 사용하였다.

1. 1시간 단위 강우자료와 유입량 자료를 3시간 평균 자료로 변환
2. 강우량과 유입량의 자기상관 및 교차상관 분석
3. 선행 유입량과 선행 강우량을 입력 변수로 사용
4. 출력값은 3시간 뒤 유입량
5. 다층 신경망 모델 사용
6. 입력 변수 수 `n`에 따라 은닉층 노드 수를 `n ~ 2n` 범위에서 실험

논문의 기본 구조는 아래와 같다.

```text
과거 유입량 + 현재/과거 강수량 → 3시간 뒤 유입량
```

본 실험에서도 이 구조를 보령댐 데이터에 맞게 적용하였다.

## 3. 사용 데이터

사용한 데이터는 기존에 만든 병합 데이터이다.

```text
보령댐_운영_강수량_병합데이터.csv
```

주요 컬럼은 다음과 같다.

| 컬럼명 | 의미 |
|---|---|
| `obsrdt` | 관측 시각 |
| `inflowqy` | 유입량 |
| `rain_mm` | 강수량 |
| `lowlevel` | 저수위 |
| `rsvwtqy` | 저수량 |
| `rsvwtrt` | 저수율 |
| `totdcwtrqy` | 총방류량 |

이번 실험에서는 논문 방식에 맞추기 위해 주로 `inflowqy`와 `rain_mm`를 사용하였다.

## 4. 강수량 CSV 구조 설명

사용자가 제공한 보령댐 강수량 CSV는 일반적인 관측값 표와 조금 다르다.

첫 줄은 다음과 같은 형태이다.

```text
format: day,hour,forecast,value  location:54_97 Start : 20221231
```

의미는 다음과 같다.

| 항목 | 의미 |
|---|---|
| `location:54_97` | 기상청 격자 위치 X=54, Y=97 |
| `Start : 20221231` | 기준 날짜가 2022년 12월 31일 |
| `day,hour,forecast,value` | 실제 데이터 행의 컬럼 구조 |

예를 들어 다음 행이 있다.

```text
31,0200,+6,0.000000
```

이 값은 다음 뜻이다.

```text
2022년 12월 31일 02시를 기준으로
+6시간 뒤인 2022년 12월 31일 08시의
강수량 값이 0.0mm
```

즉 CSV의 시간 구조는 다음과 같이 해석된다.

```text
base_time = Start 날짜 + hour
valid_time = base_time + forecast
rain_mm = valid_time에 해당하는 강수량
```

또한 파일 중간중간에 다음과 같은 줄이 반복된다.

```text
Start : 20230101
Start : 20230102
...
```

따라서 단순히 `read_csv`로 읽으면 날짜가 깨지거나 중간의 `Start` 줄이 데이터처럼 들어갈 수 있다. 그래서 이전 전처리에서는 파일을 한 줄씩 읽으면서 `Start` 날짜를 갱신하고, 각 강수량 행의 기준 시각과 예보 리드타임을 계산하여 실제 적용 시각 `valid_time`을 만들었다.

주의할 점은 이 CSV가 단순 관측 강수량이라기보다 **예보 리드타임이 포함된 강수량 자료**라는 점이다. 본 실험에서는 같은 유효 시각에 여러 예보값이 있을 경우 가장 짧은 예보 리드타임 값을 대표값으로 사용하였다.

## 5. 전처리 방식

논문 방식에 맞춰 1시간 자료를 3시간 평균 자료로 변환하였다.

### 5.1 유입량 3시간 평균

```text
inflow_mean_3h_t = inflowqy의 최근 3시간 평균
```

### 5.2 강수량 3시간 평균

```text
rain_mean_3h_t = rain_mm의 최근 3시간 평균
```

### 5.3 선행 유입량 변수

| 변수 | 의미 |
|---|---|
| `inflow_mean_3h_t` | 현재 시점의 3시간 평균 유입량 |
| `inflow_mean_3h_t_minus_3h` | 3시간 전 3시간 평균 유입량 |
| `inflow_mean_3h_t_minus_6h` | 6시간 전 3시간 평균 유입량 |

### 5.4 선행 강수량 변수

| 변수 | 의미 |
|---|---|
| `rain_mean_3h_t` | 현재 시점의 3시간 평균 강수량 |
| `rain_mean_3h_t_minus_3h` | 3시간 전 3시간 평균 강수량 |
| `rain_mean_3h_t_minus_6h` | 6시간 전 3시간 평균 강수량 |
| `rain_mean_3h_t_minus_9h` | 9시간 전 3시간 평균 강수량 |
| `rain_mean_3h_t_minus_12h` | 12시간 전 3시간 평균 강수량 |

### 5.5 목표값

목표값은 3시간 뒤 3시간 평균 유입량이다.

```text
inflow_mean_3h_t_plus_3h
```

생성 방식은 다음과 같다.

```text
target = inflow_mean_3h_t를 3시간 미래로 이동
```

즉 현재 시점의 입력 변수로 3시간 뒤 유입량을 예측한다.

## 6. 결측치 처리

임의값 대체는 사용하지 않았다.

사용하지 않은 방식은 다음과 같다.

- 평균값 대체
- 중앙값 대체
- 0 대체
- 임의 보간
- 임의 유입량 생성
- 임의 강수량 생성

3시간 평균과 시차 변수를 만들면 초반 일부 행과 마지막 일부 행에는 결측이 생긴다. 해당 행은 제거하였다.

## 7. 학습/테스트 분리

시간순 분할을 사용하였다.

| 구분 | 기간 |
|---|---|
| 학습 데이터 | 2023-01-01 ~ 2025-12-31 |
| 테스트 데이터 | 2026-01-01 ~ 2026-05-07 |

시계열 데이터이므로 무작위 분할은 사용하지 않았다.

## 8. 신경망 모델 구성

모델은 `sklearn.neural_network.MLPRegressor`를 사용하였다.

논문에서는 다층 신경망, 시그모이드 함수, 입력 변수 수 `n`에 따른 은닉층 수 `n ~ 2n` 실험을 사용하였다. 본 실험에서도 이에 맞춰 다음과 같이 구성하였다.

| 항목 | 설정 |
|---|---|
| 모델 | MLPRegressor |
| 활성화 함수 | logistic |
| 입력 정규화 | MinMaxScaler |
| 출력값 정규화 | MinMaxScaler |
| 은닉층 | 1개 |
| 은닉 노드 후보 | `n`, `1.5n`, `2n` |
| 데이터 분할 | 시간순 분할 |

출력값도 정규화한 뒤, 예측 결과는 다시 원래 유입량 단위로 되돌렸다.

## 9. 입력 변수 조합

논문의 모델 I~IV 구조를 참고하여 다음 4개 모델을 구성하였다.

### 9.1 모델 I

입력 변수 6개.

```text
Q(t), Q(t-3), R(t-3), R(t-6), R(t-9), R(t-12)
```

### 9.2 모델 II

입력 변수 7개. 모델 I에 현재 강수량을 추가하였다.

```text
Q(t), Q(t-3), R(t), R(t-3), R(t-6), R(t-9), R(t-12)
```

### 9.3 모델 III

입력 변수 7개. 모델 I에 6시간 전 유입량을 추가하였다.

```text
Q(t), Q(t-3), Q(t-6), R(t-3), R(t-6), R(t-9), R(t-12)
```

### 9.4 모델 IV

입력 변수 8개. 현재/과거 유입량과 현재/과거 강수량을 모두 포함하였다.

```text
Q(t), Q(t-3), Q(t-6), R(t), R(t-3), R(t-6), R(t-9), R(t-12)
```

여기서 `Q`는 3시간 평균 유입량, `R`은 3시간 평균 강수량이다.

## 10. 기준선

모델 성능을 판단하기 위해 단순 기준선을 함께 계산하였다.

```text
3시간 뒤 유입량 예측값 = 현재 3시간 평균 유입량
```

즉 현재 유입량이 3시간 뒤에도 유지된다고 보는 방식이다.

이 기준선은 매우 단순하지만, 유입량이 짧은 시간 동안 크게 변하지 않는 구간에서는 강력한 비교 기준이 된다.

## 11. 실험 결과

각 모델에서 MAE가 가장 낮은 은닉노드 조합을 기준으로 정리하면 다음과 같다.

| 모델 | 입력 변수 수 | 최적 은닉노드 수 | MAE | RMSE | R2 | 현재 유입량 기준선 MAE | 현재 유입량 기준선 R2 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 모델 I | 6 | 12 | 0.692 | 1.104 | 0.139 | 0.595 | 0.553 |
| 모델 II | 7 | 7 | 0.666 | 1.093 | 0.158 | 0.595 | 0.553 |
| 모델 III | 7 | 10 | 0.708 | 1.237 | -0.079 | 0.595 | 0.553 |
| 모델 IV | 8 | 16 | 0.660 | 1.134 | 0.092 | 0.595 | 0.553 |

전체 모델 중 MAE 기준으로 가장 좋은 모델은 모델 IV였다.

```text
모델 IV, 은닉노드 16개
MAE = 0.660
RMSE = 1.134
R2 = 0.092
```

그러나 현재 유입량 유지 기준선은 다음과 같았다.

```text
MAE = 0.595
RMSE = 0.796
R2 = 0.553
```

즉, 현재 실험에서는 신경망 모델이 단순 기준선을 이기지 못했다.

## 12. 결과 해석

이번 결과는 모델이 실패했다기보다, 현재 데이터와 테스트 구간의 특성을 보여주는 결과에 가깝다.

첫째, 테스트 구간인 2026년 1월부터 5월 초까지는 보령댐 유입량 변동이 상대적으로 크지 않은 구간으로 보인다. 이런 구간에서는 현재 유입량을 그대로 유지하는 기준선이 매우 강하다.

둘째, 강수량 단일 격자만으로 유역 전체 유입량을 설명하기에는 정보가 부족할 수 있다. 논문에서는 여러 T/M 강우 관측소의 강수량을 Thiessen 가중법으로 합산하여 유역 평균 강우량을 만들었다. 반면 현재 실험에서는 보령댐 주변 한 격자의 강수량만 사용하였다.

셋째, 논문은 홍수기 6월~9월의 홍수 유출사상을 중심으로 학습 자료를 구성하였다. 현재 실험은 전체 기간을 시간순으로 나누었고, 테스트 구간이 홍수기와 큰 방류 이벤트 중심이 아니다.

따라서 현재 결과만으로 “유입량 예측이 불가능하다”고 결론내리면 안 된다. 더 정확한 판단을 위해서는 홍수기 또는 유입량 급증 구간만 따로 평가해야 한다.

## 13. 가능 여부 판단

현재 보령댐 데이터와 강수량 CSV로 논문 방식의 유입량 예측은 가능하다.

가능한 이유는 다음과 같다.

- 보령댐 유입량 `inflowqy`가 존재함
- 시간 단위 강수량 `rain_mm`가 존재함
- 두 데이터가 `obsrdt` 기준으로 병합되어 있음
- 3시간 평균 유입량과 3시간 평균 강수량 생성 가능
- `Q(t+3)` 형태의 미래 유입량 목표값 생성 가능
- 신경망 학습 자체는 정상적으로 수행됨

다만 현재 성능은 아직 기준선보다 낮다.

따라서 다음 단계에서는 모델 구조보다 평가 구간과 입력 강수량 품질을 먼저 개선하는 것이 중요하다.

## 14. 다음 단계 제안

다음 실험은 아래 순서가 적절하다.

1. 보령댐 유입량 급증 구간만 따로 평가한다.
2. 홍수기인 6월~9월 데이터만 따로 학습/평가한다.
3. 강수량을 3시간 평균뿐 아니라 3시간/6시간/12시간 누적 강수량으로도 비교한다.
4. 신경망과 XGBoost 유입량 예측 모델을 함께 비교한다.
5. 가능하면 보령댐 유역을 대표하는 여러 격자의 강수량을 추가한다.

특히 현재 CSV는 단일 격자 강수량이므로, 논문처럼 유역 평균 강수량을 만들기에는 부족하다. 20개 댐 전체로 확장하기 전에 보령댐에서 여러 주변 격자 강수량을 추가할 수 있는지 확인하면 성능 개선 가능성이 있다.

## 15. 생성된 산출물

이번 실험으로 생성된 파일은 다음과 같다.

| 파일명 | 설명 |
|---|---|
| `보령댐_유입량_신경망_예측.py` | 유입량 신경망 예측 실험 스크립트 |
| `보령댐_유입량_신경망_평가결과.csv` | 모델 I~IV 및 은닉노드별 평가 결과 |
| `보령댐_유입량_신경망_예측결과.csv` | 모델별 테스트 구간 예측 결과 |
| `보령댐_유입량_신경망_예측실험보고서.md` | 본 보고서 |

